# 更新日志

[English](CHANGELOG.md)

## 9.8.20 — 2026-09-24

- 修复 Stay on the page 对浏览器右键操作的放行，涵盖链接、图片、媒体和选中文字搜索，同时继续拦截网站无关的弹出页面。

## 9.8.19 — 2026-09-24

- 小红书图片菜单的「本图」与「本帖所有图片」两组末尾均新增「原图」，分别保留单图和整帖的选择。
- 收紧 Stay on the page 的拦截范围：仅限制受保护标签页内由网站发起的跳转，放行浏览器右键菜单、用户手动输入地址，以及其他插件打开的标签页。

## 9.8.18 — 2026-09-24

- 将 Website Fixer 的网站保存成功提示显示时间缩短至 1.5 秒。

## 9.8.17 — 2026-09-24

- 修复评论图片预览通过左右按钮切换后，新图片未自动识别的问题；可用时预先分析当前图片的前后相邻图片。
- XHS Image Dark Mode 生效期间，「关闭图片」统一显示黑色，不受图片深浅色设置影响。

## 9.8.16 — 2026-09-24

- 打开小红书图片菜单时不再默认框选「关闭」，方向键仍可正常选择菜单项。
- 图片关闭后显示开关图标，再次单击即可恢复本图或整篇笔记的图片。
- 笔记打开和关闭的缩放动画期间隐藏图片按钮，图片稳定后再定位显示。

## 9.8.15 — 2026-09-24

- 优化 XHS Image Dark Mode 按钮响应、手动显示设置与资源清理，减少重复图片处理。
- 新增分组右键菜单，可关闭图片显示，并为本图或本帖所有图片选择自动、深色或浅色模式。

## 9.8.13 — 2026-09-24

- 为 White Softer 增加更浅的暖白色调，并标明原有暖米白为默认选项。
- Page Display 总开关关闭时，在插件控制窗口隐藏其两个按钮。

## 9.8.12 — 2026-09-24

- 新增 White Softer，提供四种可选色调，柔化网页中的白色背景、字体和图案。

## 9.8.11 — 2026-09-24

- 在功能说明和文档中统一使用 LeetCode Explore 全名。

## 9.8.10 — 2026-09-24

- 略微降低 LeetCode Explore 深色模式中的文字亮度。

## 9.8.9 — 2026-09-24

- 优化 LeetCode Explore 课程总览的背景、正文对比度、课程条目边框和完成图标，并统一侧栏、页脚及加载提示的深色显示。

## 9.8.8 — 2026-09-24

- 为 LeetCode Explore 的 MathJax 加载状态提示适配深色背景、文字和边框。

## 9.8.7 — 2026-09-24

- 去除 LeetCode Explore 文章加载时侧栏短暂出现的浅色亮边。

## 9.8.6 — 2026-09-24

- 修复 LeetCode Explore 章节切换时的深色模式闪屏，并去除章节总览中完成状态图标的白色阴影。

## 9.8.5 — 2026-09-24

- 优化 LeetCode Explore 深色模式中的章节简介渐隐、代码语言切换栏边框和完成状态图标。

## 9.8.3 — 2026-09-24

- 略微缩小设置页中的 Google 搜索结果语言功能图标。

## 9.8.2 — 2026-09-24

- 为 LeetCode Explore 深色模式、Instagram 关注列表检查和 Google 搜索结果语言指定功能添加图标。

## 9.8.1 — 2026-09-24

- 新增默认关闭的 LeetCode Explore 深色模式功能，跟随 LeetCode 自身外观适配课程、文章和练习。

## 9.7.11 — 2026-09-24

- 调整设置页空网站列表的留白，包括 Document Preview 白名单。

## 9.7.10 — 2026-09-24

- 修正 Any Copy Enhanced 的开发者模式信息位置，将其显示在标题行下方，而非标题右侧。

## 9.7.9 — 2026-09-24

- 设置页开发者模式新增信息行时，保持原有功能标题、图标和开关的位置不变。

## 9.7.8 — 2026-09-24

- 开发者模式中的技术标签和管辖归属标题恢复为普通灰色，并统一修正 Google 搜索功能的英文名称。

## 9.7.7 — 2026-09-24

- 调暗开发者模式中技术标签与管辖归属标题的颜色。

## 9.7.6 — 2026-09-24

- 调整开发者模式的文字层次：突出技术标签和管辖归属的标题，并使管辖归属路径与技术标签值颜色一致。

## 9.7.5 — 2026-09-24

- 新增设置页临时开发者模式：在网站输入框外按 1，可在功能名称下查看技术标签和管辖层级；再次按 1 隐藏，刷新后自动关闭。

## 9.7.4 — 2026-09-23

- 清空 Stay on the page 网站列表改为按钮内二次点击确认。

## 9.7.3 — 2026-09-23

- 为 Stay on the page 的已保存网站数量补上句号，并将保存成功提示缩短至三秒。

## 9.7.2 — 2026-09-23

- 移除 Document Preview 的「PDF 锐化」选项及相应渲染滤镜。
- 为 Website Fixer 总标题与子功能、各子功能之间添加分隔线，并在总开关关闭时灰度显示子功能。
- Stay on the page 显示已保存网站数量而不列出网站；保存成功提示在五秒后消失。

## 9.7.1 — 2026-09-23

- 在 Website Fixer 新增 Stay on the page，限制所选网站跳转或打开其他网站。
- 优化 Website Fixer 卡片布局，提供独立的子功能控制；Stay on the page 的已保存网站列表不直接显示。

## 9.6.30 — 2026-09-23

- 清空错误的网站规则输入后，立即清除相应的校验提示。
- 接受使用未列入清单的顶级域名的网站地址，并改进粘贴地址的处理。

## 9.6.29 — 2026-09-23

- 将域名列表的子域名覆盖说明移至列表末尾，并简化文案。
- 加强 Translate Override 对所选网站的页面级与内容级禁止翻译标记的处理。
- 消除设置页空网站列表附近多余的留白。

## 9.6.28 — 2026-09-23

- Satellites 的网站域名输入框现可接受粘贴的网址，并自动识别对应站点；直接输入域名的原有规则保持不变。
- 缩小设置页各处域名输入框与已保存网站列表之间多余的空白。
- 已保存的网站列表有内容时，在列表上方统一说明子域名覆盖范围，不再逐项重复标注。

## 9.6.27 — 2026-09-23

- 在 Satellites 新增 Website Fixer，其 Translate Override 子功能可对所选域名及其子域名忽略网站的禁止翻译声明，实际翻译仍由 Chrome 负责。

## 9.6.26 — 2026-09-23

- No Autoplay 总开关关闭时，声音自动播放设置灰度显示且不可操作，已保存的选择保持不变。

## 9.6.25 — 2026-09-23

- 调整 Native Scroll 和 No Autoplay 建议设置说明的英文措辞。

## 9.6.23 — 2026-09-23

- 调整 Native Scroll 和 No Autoplay 建议设置说明的中英文表述。

## 9.6.22 — 2026-09-23

- 调整 Native Scroll 设置页的中文说明。

## 9.6.21 — 2026-09-23

- 新安装时所有功能均默认关闭，包括此前自动启用的防护功能及 Website Knowledge Control 子选项；已有用户的选择保持不变。
- 移除 Native Scroll 和 No Autoplay 设置页中多余的默认状态说明。

## 9.6.20 — 2026-09-23

- 在受 Access Control 限制的页面点击插件工具栏按钮即可单次放行，并移除原控制窗口中的放行按钮。

## 9.6.19 — 2026-09-23

- 优化项目文档表述。

## 9.6.18 — 2026-09-23

- Document Preview（含 PDF 预览）中的网页和应用外链先显示确认浮窗；邮件、电话和短信链接仅提供复制。
- 与 Mailto Capture 共用协议解析，同时保留文档隔离和不安全内容拦截。

## 9.6.17 — 2026-09-23

- 修复隔离阅读器中的 PDF 后台解析线程启动和整份文档复制问题，并优化跨框架弹窗聚焦。
- 移除 PDF 滚动时出现在页面内的加载图标，仅在当前可见页需要较长绘制时间时使用工具栏分割线显示进度，不移动文档布局。

## 9.6.16 — 2026-09-23

- 所有设置与各功能设置页统一只保留最新打开的一个标签页，自动关闭此前的设置页，不影响扩展的其他页面。
- 将 Document Preview 弹窗的加载指示放在「预览」按钮左侧，加载时不再增高或移动弹窗内容。

## 9.6.15 — 2026-09-23

- 缩小 PDF 文档信息窗口并保持文字大小不变，修复内容较长时自动滚到底部的问题，每次打开均从顶部开始显示。

## 9.6.13 — 2026-09-23

- 优化 PDF 阅读器工具栏的居中布局、间距、对称页码显示和缩放菜单，并保留上一页／下一页按钮。
- 将外观切换合并为单个按钮，保留原有悬停提示。

## 9.6.12 — 2026-09-23

- 支持点击 PDF 阅读器顶部文件名查看文档信息，日期采用 YYYY-MM-DD 格式，保留文件提供的秒数，并以简写方式显示时区。
- 并行准备 PDF 阅读器与文档数据，优先显示第一页。
- 移除 PDF 阅读器的内置搜索按钮与功能，将查找交由 Chrome 处理。

## 9.6.11 — 2026-09-23

- 将 PDF 阅读器的向左旋转图标更新为圆角方框配逆时针箭头，并统一与其他工具栏图标的尺寸和对齐。

## 9.6.10 — 2026-09-23

- 将 Document Preview 图标改为 Cosmic PDF 的文档图案，保留 Cosmic Gemini 原有的图标配色和样式。

## 9.6.9 — 2026-09-23

- 将 PDF 锐化改为独立勾选项，默认关闭。取消勾选也会移除已打开阅读页的锐化效果，无需重新创建画布。
- 新增 1×/3×/5× PDF 采样选项，默认仍为 4×。1× 同时使用更小的页面画布上限，以降低内存占用。

## 9.6.8 — 2026-09-23

- 在 Document Preview 设置中新增 PDF 采样下拉菜单，提供 2×/4×/6×，默认 4×。修改仅应用于新打开的 PDF 阅读页面。
- 提升高倍缩放清晰度，并为扫描页面加入轻度锐化；深色模式保留此前的对比度。高清渲染集中于可见区域，页面缓存继续保持资源上限。

## 9.6.7 — 2026-09-23

- 提升 PDF 文字与细线的显示质量，优化非整数缩放比例下的渲染，并继续限制画布与页面缓存占用。深色模式使用更黑的纸张和更明亮的文字，保留清晰的页面边界。

## 9.6.6 — 2026-09-22

- 提升 PDF Viewer 在高像素密度屏幕上的清晰度：普通页面与侧栏缩略图以更清晰的像素密度绘制，同时保留高倍缩放的局部高清渲染和画布资源上限。

## 9.6.5 — 2026-09-22

- 将 Document Preview 的 README 简介改为面向首次访问者的功能概述，只保留支持的文件类型和主要预览限制；具体操作与生命周期规则仍见设置页和技术文档。
- 修复电子表格预览的吸顶标题：滚动时不再从列标题上方露出单元格，也不会覆盖左上角的空白格。工具栏在来源网站旁显示本地化的文件类型。

## 9.6.3 — 2026-09-22

- 精简中英文 README 中 Document Preview 与 XHS Image Dark Mode 的说明；将设置页中 XHS Image Dark Mode 的三段说明合并为一个自然段，实验性提示继续单独显示。

## 9.6.2 — 2026-09-22

- PDF 首次打开和刷新后停在文档最顶部，完整显示第一页与工具栏之间的留白；正常翻页、缩放与外观切换保持原有的阅读位置行为。

## 9.6.1 — 2026-09-22

- 新增内部 PDF Viewer，为 Document Preview 提供 PDF 阅读能力，暂不在设置页单独显示。支持文字选择、搜索、页码跳转、缩略图、文件目录、适合宽度／页面、全屏、密码保护的 PDF、打印和下载原文件，不提供编辑或绘图工具。
- 新增独立的 PDF 深浅色阅读外观、清晰的纸张边线和仅影响显示的向左旋转；默认以 100% 打开，加减缩放每次调整 10 个百分点。优化居中工具栏，采用无边框按钮、分组竖线与紧凑的 M / N 页码显示，并避免深色模式加载时出现浅色画面。
- 优先渲染可见页面，连续缩放停稳后再补充清晰度，并限制离屏页面和缩略图的资源占用。PDF 在本地独立工作线程与无扩展权限的沙箱中处理，不运行文档脚本或加载远程内容。

## 9.5.8 — 2026-09-22

- 优化 Word 系列预览（`.doc/.docx/.docm/.dotx/.dotm`）：完整显示连续空格与带下划线的空白填写区域，修正直接文字样式的覆盖优先级，并完善字符位置、段落边框与间距及表格格式。
- 新增常见 Word 公式、注音和复选框符号的静态显示，保留图片的原设定尺寸，并补充旧版 Word 内嵌位图、字符间距和安全文档链接的支持。转换仍在本地进行并限制处理规模，不运行宏或加载远程内容。
- 重点完善 Word 列表：改善长条目换行对齐，保留样式关联的多级层次、续编号与重新编号，并补充项目符号及中文编号格式；同时修正旧版 PowerPoint 中相关的内嵌位图记录识别。
- 文档预览在来源标签页的紧邻右侧打开，包括本次网站访问中记住的预览选择。
- 浏览器翻译时，预览工具栏、文件名和来源网站信息保持原样。

## 9.5.7 — 2026-09-22

- 将 Document Preview 在预览页关闭后的临时缓存保留时间缩短至 10 分钟。

## 9.5.6 — 2026-09-22

- Document Preview 的临时缓存改为在最后一个预览页关闭 30 分钟后清理；来源网站会话结束或 Chrome 退出时仍会提前清理。

## 9.5.5 — 2026-09-22

- 将文档信息合并到预览工具栏：Cosmic Gemini 显示在 Document Preview 下一行，加宽中间区域并稍微缩小文件名字号，文件名只显示一行，下一行显示来源网站，不再显示文件大小。超长文件名以省略号收起，悬停可查看完整名称。稍微增加工具栏高度，并适配窄窗口；PDF 预览继续隐藏插件工具栏。

## 9.5.3 — 2026-09-22

- 显示 PDF 时自动隐藏插件工具栏和文件信息栏，让 Chrome 原生阅读器占满预览区域；其他文档格式保留原有顶部区域。

## 9.5.2 — 2026-09-22

- Document Preview 新增 Dark Reader 式 PDF 深色模式，沿用现有外观偏好与手动切换按钮，切换时无需重新加载文档。继续使用 Chrome 原生阅读器；显示滤镜也会改变内嵌图片、阅读器控件与背景的颜色，原文件保持不变。

## 9.5.1 — 2026-09-22

- 优化 Document Preview 对旧版 `.doc/.xls/.ppt` 的显示，保留常见字体与文字样式、段落间距和缩进、列表编号、表格尺寸与单元格格式，以及支持的幻灯片文本框、颜色和内嵌图片。
- 改进非 PDF 预览：补充电子表格富文本，修复隐藏行列附近的合并单元格显示，完善 RTF/OpenDocument 格式与幻灯片文字布局，并安全保留邮件的常见显示样式。
- 修正字体继承及中西文字体选择，原字体不可用时使用本地替代字体。
- 修正 Word 段落间距，保留空白填写区域、标题空格及默认制表宽度。
- 文件大小改用十进制单位，最多保留一位小数，未换算的大小使用 byte/bytes 并区分单复数。
- 修复预览默认样式覆盖文档格式的问题；复用图片和格式数据、减少重复解析，并限制复杂版式的处理规模，兼顾本地转换性能。仍不加载远程内容或运行可执行内容，PDF 继续由 Chrome 原生阅读器处理。

## 9.4.1 — 2026-09-22

- Document Preview 新增 `.doc/.xls/.ppt/.rtf/.odt/.ods/.odp/.eml` 支持，邮件格式归入支持列表的「其他」分类；旧版 Word 和 PowerPoint 文件以内容预览为主。
- 点击「预览」后立即显示加载线条和状态，不显示百分比数值；微调支持格式列表的折叠标识，移除文档信息栏中的格式差异提示。
- 加强文档的本地解析、静态内容隔离与远程资源拦截；PDF 继续使用 Chrome 原生阅读器。
- 加固 Customs Province 的处理器授权、文档缓存传输、原文件下载及媒体处理资源限制。
- Document Preview 新增自动包含所有子域名的网站白名单，支持有序管理与输入帮助；Access Control 和 Document Preview 的域名列表改为双列，仅在窄窗口下使用单列。

## 9.3.1 — 2026-09-22

- Document Preview 转由 Customs Province 管理，设置页位置保持不变。
- Document Preview 新增电子表格、演示文稿和 PDF 预览，涵盖 Office 模板、放映及含宏格式；新增工作表与幻灯片切换，并接入 Chrome 原生 PDF 阅读器。
- 简化功能简介，在设置页新增默认折叠的格式分类列表；本次网站访问的操作偏好统一适用于所有支持的格式。
- 新增有资源上限的本地 Office 转换与无脚本预览，不运行宏、嵌入程序、远程内容或电子表格公式。
- 文档改为仅缓存在浏览器内存中，在最后一个预览页关闭 3 小时后、来源网站会话结束时或 Chrome 退出时清理，以先到者为准。

## 9.2.3 — 2026-09-22

- Document Preview 改为仅在用户明确选择「预览」或「下载」后，才在本机获取、验证并缓存文档；关闭选择框不会留下文档缓存。
- 调整外观设置的交互范围，说明文字不再触发下拉菜单；同时精简双语说明并去除该行下方的多余间距。

## 9.2.2 — 2026-09-22

- 在 Document Preview 设置中新增「自动／浅色／深色」外观菜单。预览中手动选择的外观会在本次网站访问期间对来源网站及其子域名持续生效；点击「默认」可恢复设置页中的偏好。
- 将深色文档的默认文字颜色调整为与文件名一致的较亮白色，改善阅读体验。
- 文件信息区的临时操作提示会在 15 秒后自动隐藏，加载中、渲染失败及预览失效等状态继续保留。
- 新增缓存清理期限：每份文档的最后一个预览页关闭 10 小时后，或来源网站的本次访问结束时，按先到的条件清理。到期前重新打开预览，会在预览仍打开时取消倒计时。

## 9.2.1 — 2026-09-22

- 优化 Document Preview 的阅读版式，并更好地保留 Word 常见格式，包括字体、文字颜色、段落对齐与间距、缩进、列表编号，以及表格列宽、边框和底色。文档颜色同时适配深色模式，保持可读性。
- 为显式分页符和设置了段前分页的段落添加可见分隔线。
- 新增仅作用于正文的缩放控件，以 10% 为步长在 50% 至 200% 之间调整；点击百分比即可恢复 100%，无需重新加载文档。
- 加强文件信息区与文档正文之间的分界，同时保留紧凑的顶部区域和较大字号、最多两行的文件名。

## 9.1.3 — 2026-09-22

- 增大 Document Preview 文件名的字号，长文件名仍最多显示两行。

## 9.1.2 — 2026-09-22

- 在保留现有视觉风格的同时，压缩 Document Preview 顶部工具栏和文件信息区的占用空间。文件信息保留清晰易读的字号，长文件名最多换行为两行。正文自动填满窗口剩余高度，并优化小窗口中的控件排版。

## 9.1.1 — 2026-09-22

- 新增 Document Preview，默认关闭，归属 Standing Province，位于 Satellites 中 Ad Marshal 上方。支持的 DOCX 下载可在询问保存位置前选择本地预览或下载原文件，配有文档图标、中英文界面，并在大小已知时显示文件大小。
- 新增按 eTLD+1 跨子域名共享的本次操作偏好和文档临时缓存，网站最后一个标签页关闭或离开后统一清理；控制窗口中的动态按钮可恢复每次询问。
- 文档预览自动适配深色与浅色外观，并提供手动切换按钮，包括正文和表格；内嵌图片及原始 DOCX 文件保持不变。关闭接管功能后，已有预览仍保留至来源网站的本次访问结束。
- 简化 Document Preview 与 XHS Image Dark Mode 在设置页中的中英文说明，并保留 XHS 独立的实验性功能提示。

## 8.14.3 — 2026-09-22

- 仅缩小插件控制窗口中的 Access Control 单次访问图案，使其与传统功能图标的视觉尺度一致，同时保留原有按钮尺寸和点击区域。

## 8.14.2 — 2026-09-22

- Access Control 新增默认不勾选的单次访问选项。勾选后，当前页面被阻止访问时，插件控制窗口会显示动态按钮，可让当前标签页临时访问匹配域名而不修改已保存的阻止列表；标签页离开该受限域名或被关闭后，临时放行自动失效。

## 8.14.1 — 2026-09-21

- 新增 Search Result Language Designate for Google Search（`lang-google`），归属 Standing Province，位于 Satellites 最后一项，默认关闭。支持将不区分大小写的 `lang:` 搜索指令转换为 Google 搜索结果语言筛选参数，支持简繁中文别名、多种语言及视频等搜索分类。

## 8.13.31 — 2026-09-21

- 将上下文输入面板中的别名栏目更名为「网站别名」并移至「快捷指令」之后；当前网站规则输入框没有别名时会隐藏整个栏目，点击面板外部也会关闭面板。

## 8.13.30 — 2026-09-21

- 为设置页中的全部网站规则输入框新增上下文说明；输入 `?` 或 `？` 会打开本地化面板，说明当前输入框接受的内容与快捷指令，并且不会将帮助字符保存为规则。

## 8.13.29 — 2026-09-21

- Access Control 新增部分服务的简短输入别名；别名会解析为相应根域名，并沿用现有的全部子域名覆盖方式，不会增加额外的后台规则。

## 8.13.28 — 2026-09-21

- 将墨西哥电话号码 2 至 9 区的数字标识替换为官方方向名称，并跟随界面语言显示。
- NANP 归属地继续只显示英文；其他国际号码的国家、地区与业务名称会在中文界面中显示规范简体中文。中国座机归属地改用不含行政区划通名的国家、省级区域与城市简称。
- 中国号码的英文归属地会保持“省级区域 + China”不拆行，中文归属地会保持“中国 + 省级区域”不拆行，城市名称仍可自然换行。

## 8.13.27 — 2026-09-21

- 优化 Mailto Capture 的归属地换行：较长的城市或都会区名称可自然换行，末尾的州名与 `USA` 始终保持为一个整体；当前行仍放得下时，不会强制移至新行。
- 为 Mailto Capture 新增预编译的紧凑国际电话号码索引，可识别国家或地区并按照相应编号计划格式化显示；中国座机号码还会识别区号归属地且不打包手机精确号段数据，墨西哥号码则会显示首位号码对应的大区。
- 让 North American Numbering Plan 标识在自然换行时保持完整，统一政府服务标识，并为设置说明中的 `tel:` 与 `sms:` 标识添加引号。

## 8.13.26 — 2026-09-21

- 将 Mailto Capture 号码下方的区号归属信息调整为与电话号码相同的字号，同时保留灰度区分。

## 8.13.25 — 2026-09-21

- 统一 Mailto Capture 小窗口中的 NANP 电话号码格式，将已识别的区号归属信息移至对应号码下方，并取消单独的归属信息栏目；无法识别的号码不再显示归属内容或错误说明。

## 8.13.23 — 2026-09-21

- 将 Mailto Capture 仅英文显示的区号归属结果中的 `United States` 缩写为 `USA`。

## 8.13.22 — 2026-09-21

- 优化 Mailto Capture 仅英文显示的 `Area code location` 字段并改用紧凑的离线混合索引：能够可靠定位城市或都会区时仅使用三位区号，覆盖范围较大的区号则使用预编译 NPA-NXX 明细；没有精确已分配前缀时保留区域级回退。

## 8.13.21 — 2026-09-21

- Mailto Capture 现可预览并复制 `sms:` 链接中的收件号码与短信内容，为北美号码提供仅英文显示的离线归属信息，并恢复原有功能说明，将支持的外部协议链接改为单独补充说明。

## 8.13.20 — 2026-09-21

- Mailto Capture 现可拦截 `tel:` 链接，在本地预览电话号码并复制完整拨号目标，无需直接打开系统应用。

## 8.13.19 — 2026-09-21

- 在 Ad Marshal 设置中明确知乎适配覆盖 `zhihu.com` 及其子域名。

## 8.13.18 — 2026-09-21

- 优化 Claude 浏览器标识固定化设置的简体中文说明。

## 8.13.17 — 2026-09-21

- 将按语言自适应的精确主机与通配规则说明布局统一应用到所有网站规则区域。

## 8.13.16 — 2026-09-21

- 优化 Any Copy 网站规则说明的换行方式：英文固定分行，中文仅在空间不足时自然换行。

## 8.13.15 — 2026-09-21

- 将 No Autoplay 声音自动播放网站规则下方的精确主机与通配规则说明拆分为两行，与其他位置保持一致。

## 8.13.13 — 2026-09-21

- 网站规则输入框现会为前导点域名及共享 `etld` 参考列表中的多标签条目自动补全通配规则，同时排除单标签顶级域。

## 8.13.12 — 2026-09-21

- 优化共享 eTLD 参考列表中部分规则的内部分类。

## 8.13.11 — 2026-09-21

- 对共享列表中的 PSL PRIVATE DOMAINS-sector geographic eTLD rules 完成通用化与覆盖补全，且暂不接入任何产品决策。

## 8.13.10 — 2026-09-21

- 新增共享的 `etld` 参考列表，取自当前 Public Suffix List 的 ICANN 区段，并保留部分指定的 PRIVATE 规则，供后续功能使用。

## 8.13.9 — 2026-09-21

- 用户维护的网站规则现支持精确的 IPv4 和 IPv6 地址（包括 Access Control），并可跨端口匹配。

## 8.13.8 — 2026-09-21

- 新增带二次确认的 `clean` 输入指令，可清空规则卡片管理的全部域名记录。

## 8.13.7 — 2026-09-21

- 输入框为空时长按“添加”，或输入 `reset` 后提交，均可将用户维护的域名列表按字母顺序重排；之后新增的域名仍会列在末尾。

## 8.13.6 — 2026-09-21

- 所有由用户维护的域名列表现均按添加先后顺序排列，不记录时间信息。

## 8.13.5 — 2026-09-21

- 将共享垃圾桶图标调整为更宽、更均衡的造型。

## 8.13.3 — 2026-09-21

- 将 Access Control 光标图标的尾线与光标尖端及内凹顶点调整至同一直线。

## 8.13.2 — 2026-09-21

- 为 Satellites 中的 Mailto Capture、Clipboard Protect、Access Control、Website Knowledge Control、Ad Marshal、XHS Image Dark Mode 和 Bili Daily Login 卡片添加各自的功能图标；其余两项网站专用功能暂时保持纯文字标题。
- 将英文 README 中的 Satellites 分组标题改用半字线。

## 8.13.1 — 2026-09-21

- 在 Standing Province 下新增 Access Control，通过总开关和本地域名规则阻止访问所列域名及其所有子域名；规则在匹配页面刷新或再次打开后生效。Clipboard Protect 现紧随 Mailto Capture，中英文 README 也将 Satellites 整理为“通用功能”和“网站专用功能”。
- 将 XHS Image Dark Mode 的功能说明按用途、图片分析、评论区图片和操作方式重新分段，并同步调整中英文界面与 README。

## 8.12.27 — 2026-09-21

- 将 Satellites 分类标题的字号调整为与功能卡片标题一致，同时保留分组分隔线。

## 8.12.26 — 2026-09-21

- 将 Satellites 设置页划分为“通用功能”和“网站专用功能”；Ad Marshal 现位于 Clipboard Protect 正下方，并作为“通用功能”分组的最后一项。

## 8.12.25 — 2026-09-21

- 清理控制窗口中冻结的展示代码，包括永久隐藏的主 wordmark 和 Image Download 已停用的“推荐”标记接口，并删除未使用的共享图标定义；当前实际显示的 Video Download 面板标识保持不变。

## 8.12.23 — 2026-09-20

- Instagram 的取消关注操作现在只负责打开 Instagram 原生确认对话框，不再代替用户选择任何选项。用户完成操作后，或账号已在其他窗口或设备被取关时，只有重新搜索关注列表并确认该账号不存在，侧边栏才会将其标记为“已取关”。

## 8.12.22 — 2026-09-20

- 修复 Page Display 在采用严格内容安全策略的页面上失效的问题；Reduce White Point 与 Greyscale 现在通过 Chrome 保护的用户来源样式路径应用，同时保留对页面反转的亮度补偿与关闭后的完整恢复。

## 8.12.21 — 2026-09-20

- Instagram 取消关注现在只在重新打开 Following 并通过稳定的新搜索确认找不到该账号后才视为完成；旧行暂时消失或主页计数变化不再触发成功状态，确认失败后仍可重试。

## 8.12.20 — 2026-09-20

- 修复 Instagram 取消关注确认流程，页面主题不再能通过改变按钮颜色导致工具误选“取消”；现在依据原生确认弹窗结构，并继续要求操作后的关系验证。

## 8.12.19 — 2026-09-20

- 修复 Instagram 虚拟列表持续产生像素高度变化时，已经识别完成的列表仍循环扫描的问题；现在依据到达实际底部且账号集合稳定来结束。

## 8.12.18 — 2026-09-20

- 通过两次完整的重叠复核和自适应视口稳定等待加强 Instagram 列表覆盖，确保较慢的虚拟列表重挂载完成采集后再继续扫描。

## 8.12.17 — 2026-09-20

- 将 Instagram 列表的完整快速复核应用到每次识别，不再受页面显示数量或 DOM 保留状态影响；同时批量执行从顶部到底部的重叠复核，减少漏读账号以及反复等待后台调度的时间。
- macOS 在源代码目录中重新生成 Finder 元数据文件时，不再干扰扩展检查。

## 8.12.16 — 2026-09-20

- 为 Instagram 侧边栏中的每个账号统一保留一行显示名称空间，包括没有设置自定义显示名称的账号。

## 8.12.15 — 2026-09-20

- 简化 Instagram 账号识别结果，直接从经校验的主页跳转目标中提取一份 user handler，并由其生成侧边栏中的主页链接。即使 Instagram 显示的数量已滞后，虚拟列表也会在结束前执行一次完整复核，以减少漏读账号和关注关系误判。

## 8.12.13 — 2026-09-20

- 改为仅从 Instagram 实际显示的文本读取已验证账号的 user handler，并明确侧边栏中清除当前主页识别数据的操作文案。

## 8.12.12 — 2026-09-20

- 移除 Instagram 账号行中残留的验证文字，防止网页中隐藏的验证标签被读取为账号显示名称，并将带蓝勾的账号置于未回关列表末尾。

## 8.12.11 — 2026-09-20

- 将 Instagram 侧边栏中的验证徽章改为与 user handler 留有间距的蓝色文字勾，并移除可见的悬停说明文字。

## 8.12.10 — 2026-09-20

- 将 Instagram 账号在侧边栏中的自定义显示名称字号调整为与 user handler 相同，同时保留次要文字颜色和独立分行布局。

## 8.12.9 — 2026-09-20

- Following/Follower List Check for Instagram 现会在读取网页列表时识别账号旁的小型蓝色验证标记，并在侧边栏对应账号的可见 user handler 后显示一个蓝色勾。

## 8.12.8 — 2026-09-20

- Instagram 列表现以已加载列表抵达稳定底部作为完成依据，不再要求扫描结果与 Instagram 显示的账号数量严格一致。数量不一致时，保留在 DOM 中的账号行会被直接复核；只有虚拟列表才会进行一次带重叠区域的快速重新挂载扫描。显示数量暂未更新时，已完成的进度也不再停留在 99%。

## 8.12.7 — 2026-09-20

- 调整 Following/Follower List Check for Instagram，账号显示、去重及列表对比现统一采用页面上可见的 user handler。账号链接改为单独保留，供侧边栏点击账号时跳转，包括链接路径与可见 handler 不一致的情况。

## 8.12.6 — 2026-09-20

- 修复 Instagram 列表检查可能始终少一个账号并停留在 99% 的问题。账号身份改为直接采用 Instagram 账号链接中的 user handler；滚动读取增加重叠检查点，并在虚拟列表接近完成时进行一次更细步长的完整复核，再决定是否进入列表不完整恢复流程。
- 调整 Satellites 中 IG± 启动说明的简体中文表述。

## 8.12.5 — 2026-09-20

- 调整 Following/Follower List Check for Instagram 在设置页面中的使用说明和隐私说明表述。

## 8.12.3 — 2026-09-20

- 将 Following/Follower List Check for Instagram 的完整结果保留于本次浏览器会话内存，再次打开同一主页时可恢复经校验的结果，直至退出浏览器。完成结果底部新增清除当前主页数据和清除所有 Instagram 列表识别数据的操作。
- 提升 Instagram 列表读取在网络较慢时的容错能力：延长等待时间，主动重新唤醒停滞的虚拟列表，并可根据重新核验的主页计数最多重新扫描两次后才报告列表不完整；不完整的对比结果仍不会显示。

## 8.12.2 — 2026-09-20

- 重做 Following/Follower List Check for Instagram 在本人主页上的取消关注流程，改为使用 Instagram 页面中可见的关注列表、精确用户名搜索及原生确认控件，不再依赖内部请求接口。原有的本人主页、明确确认及关注关系校验继续保留；结果不确定的写入不会自动重复。
- 在 Satellites 中将该功能标记为“点击时”。Instagram 检查运行期间打开插件控制窗口，不再把其侧边栏切换成 Image Download；读取控制窗口状态也不再占用任何产品的工作区。

## 8.12.1 — 2026-09-20

- 在 Satellites 中新增 Following/Follower List Check for Instagram，通过 Instagram 网页上的 IG± 控制窗口按钮打开侧边栏，对比关注与粉丝列表。两份列表在各自开始读取后显示独立进度条。列表读取不依赖界面语言，收起侧边栏后仍继续进行，仅在完整读取两份列表后显示对比结果。在自己的主页上，可逐个确认取消关注，执行前会核实登录账号及当前关注关系。
- 修复侧边栏切换，确保 Image Download 打开自己的工作区，且清理时不会关闭其他工具的侧边栏。

## 8.11.19 — 2026-09-20

- 改进 No Autoplay 对自定义媒体播放器的兼容性。用户明确操作播放控件后，现会向同一播放器中的媒体授予有时限的播放意图，包括需要异步完成准备的播放；被拦截的脚本播放也不再错误地报告成功，避免播放器因此卡在错误状态。

## 8.11.18 — 2026-09-20

- 降低 XHS Image Dark Mode 在长瀑布流与已展开笔记中的持续运行开销。新增内容改为局部注册，只有 DOM 节点被移除后才安排全记录清理；评论图片预览与用户主页按钮则改用专用索引及 URL 状态缓存，避免反复扫描或重写。图片采样尺寸、提前处理范围与识别规则均保持不变。

## 8.11.17 — 2026-09-20

- 改进 XHS Image Dark Mode 图片按钮的操作可靠性，完整隔离按钮指针手势与小红书轮播手势。已隐藏的预览层不再保留重叠按钮，且只有当前实际可见的评论图片预览可以隐藏笔记图片按钮。

## 8.11.16 — 2026-09-20

- 修复已脱离页面的 XHS 评论图片预览按钮在短时间内拦截本应传递给笔记图片按钮的操作；评论缩略图点击识别现在也只检查对应的评论容器。

## 8.11.15 — 2026-09-20

- 修复 XHS Image Dark Mode 在评论图片预览使用不同资源或复用页面既有元素时无法处理的问题。打开的评论图片现在会独立识别并提供自己的浅色／深色切换按钮，同时继续隐藏笔记图片按钮。

## 8.11.13 — 2026-09-20

- XHS Image Dark Mode 现会在笔记展开后支持评论区图片，且仍按可视区域按需处理；打开评论图片预览时，笔记图片按钮会暂时隐藏，并在预览关闭后恢复。

## 8.11.12 — 2026-09-20

- 调整 XHS Image Dark Mode 的图片按钮：长按会在整篇笔记强制深色与强制浅色之间切换，单击则恢复该笔记全部图片的自动识别；瀑布流封面及稍后载入的图片也会保持一致。

## 8.11.11 — 2026-09-19

- 扩展 XHS Image Dark Mode 的识别能力，现可处理包含多块明亮对话区域的聊天截图，同时继续保留带有单一明亮主体的照片。

## 8.11.10 — 2026-09-19

- 改进 XHS Image Dark Mode 对鲜艳纯色背景上明亮文字的识别，同时继续保留带有独立明亮主体的照片内容。

## 8.11.9 — 2026-09-18

- 调整 XHS Image Dark Mode 的笔记级长按操作：暂停处理当前笔记后，图片切换按钮会直接隐藏，瀑布流中的对应封面也会恢复原始显示，再次打开同一笔记时仍保持暂停。用户主页现另有开关，可暂停识别和调整该主页中的全部笔记。
- 清理 Ad Marshal 内部未使用的代码。

## 8.11.8 — 2026-09-18

- 调整 XHS Image Dark Mode 的长按操作，改为暂停处理当前打开的笔记。全部图片会恢复原始显示，稍后载入的图片也保持不变，图片按钮则改为关闭状态的开关图案。再次长按会恢复该笔记的自动处理；暂停期间短按不可用。

## 8.11.7 — 2026-09-18

- 为 XHS Image Dark Mode 的图片按钮增加长按操作。长按会将本次浅色或深色切换应用到当前笔记的所有图片，包括稍后载入的图片；再次长按则恢复每张图片各自的自动识别结果。短按仍只影响当前图片。

## 8.11.6 — 2026-09-18

- 修复小红书查看器中的提示层和覆盖内容被误认为帖子图片，导致浅色、深色按钮在同一位置重叠并使可见按钮看似无效的问题。图片按钮现在只关联实际图集媒体，且每个查看器最多显示一个按钮。

## 8.11.5 — 2026-09-17

- 在 Website Knowledge Control 的简体中文（中国）之后新增简体中文（无地区）`zh-Hans` 与繁體中文（無地區）`zh-Hant`。所选语言会同时用于请求语言、浏览器语言信息及默认地区格式。

## 8.11.3 — 2026-09-17

- 修复 Clipboard Protect 只有刷新 Satellites 后才会显示的问题。刷新载入与设置页内部导航现在会呈现相同的控件，并新增自动化检查，防止两种载入路径的完整页面内容和导航结构再次发生偏差。

## 8.11.2 — 2026-09-17

- Any Copy 在当前网页生效时，Clipboard Protect 现在会自动让路。Central 会先停用原有的复制保护，再启动接管的一方；当 Any Copy 不再在该网页生效时，Clipboard Protect 会自动恢复。

## 8.11.1 — 2026-09-17

- 在 Satellites 的 Website Knowledge Control 下方新增 Clipboard Protect。通过独立开关保护选中并复制的原文，阻止网站在复制时夹带其他内容，并尽量保留原有格式；可编辑区域继续使用网站自身的复制方式。可与 Any Copy 同时使用，默认关闭。

## 8.10.11 — 2026-09-17

- 在 Ad Marshal 中勾选知乎后，Central 现在会在 `zhihu.com` 及其全部子域名中持续开启 Any Copy。控制窗口中的 Any Copy 按钮可仅为当前标签页暂停或恢复功能，不会改动持久的管制网站选择，也不影响其他知乎标签页。

## 8.10.10 — 2026-09-17

- 修复 Website Knowledge Control 在不提供 `crypto.randomUUID()` 的 HTTP 页面中无法初始化的问题。页面桥接现在与其他页面运行代码一样，改用安全随机字节作为后备方案。

## 8.10.9 — 2026-09-17

- 修复小红书详情首图已被识别为文字卡片后，瀑布流封面偶尔仍保持浅色的问题。详情图的较高质量识别结果现在会取代裁剪封面先前的照片判定，并立即同步到仍在页面中的瀑布流封面。

## 8.10.8 — 2026-09-17

- 修复小红书详情图集中前景文字较少的页面被误判为照片的问题。分类器现在可以识别高度一致的明亮阅读底面上彼此分离的文字笔画，同时继续保留独立的小型照片主体。

## 8.10.7 — 2026-09-17

- 修复小红书详情图集偶尔遗漏已完成解码的第二张及后续图片的问题。查看器中新插入的图片现在会立即进入高优先级分析队列，响应式图片换源也会持续关联到所属页面；同时支持识别透明阅读底面的文字图片，并继续保留独立的透明照片主体。

## 8.10.6 — 2026-09-17

- 将 Website Knowledge Control 的语言与 Intl 地区设置合并为同一个“语言与地区格式”选项，由一次选择统一控制网络请求、浏览器语言信息及默认 Intl 格式；并在中国香港之后新增中国澳门、中华台北、马来西亚和新加坡的中文地区选项。

## 8.10.5 — 2026-09-16

- 为 Mailto Capture 弹窗添加与进入效果相呼应的短暂退出动画，并遵循系统的减少动态效果偏好。

## 8.10.4 — 2026-09-16

- 去除 Global Privacy Control 设置项下方多余的留白。

## 8.10.3 — 2026-09-16

- 在 Website Knowledge Control 中新增可独立勾选的 Global Privacy Control。启用产品后，该选项会随网页请求发送 `Sec-GPC: 1`，并向页面提供 `navigator.globalPrivacyControl`；与其他浏览器信息设置一致，修改后的状态会在页面下次载入时生效。

## 8.10.2 — 2026-09-16

- 在 Website Knowledge Control 时区菜单的三级置顶组中新增新加坡。

## 8.10.1 — 2026-09-16

- 在 Satellites 的 Mailto Capture 下方新增 Website Knowledge Control。通过总开关和独立的语言、Intl 地区格式及时区选项，自行设置网站可获取的浏览器信息；语言菜单与 Intl 地区设置共用同一组选项；新功能默认关闭。
- 隔离普通窗口与无痕窗口的请求语言规则，并协调浏览器信息调整，避免不同设置相互覆盖。
- 每个已载入页面会继续使用最初取得的 Website Knowledge Control 配置；修改或关闭设置后，只在页面下次载入时恢复浏览器原有信息。

## 8.9.28 — 2026-09-16

- 将 Claude 浏览器标识一致性调整扩展至网络层。这项独立设置生效时，打开受支持 Claude 与 Anthropic 网站的顶层导航请求，以及由这些页面发起的网络请求，现在仅使用 `Accept-Language: en-US`；限定范围的规则沿用现有的标签页保留周期，并会在最终清理或重置时移除。

## 8.9.27 — 2026-09-16

- 调整设置页面中 Chinese Response Display Optimization for Claude 的功能说明表述。

## 8.9.26 — 2026-09-16

- 明确说明 Chinese Response Display Optimization for Claude 只调整用户浏览器中显示的回复文字，排除的内容保持不变。

## 8.9.25 — 2026-09-16

- 修复 Cosmic Gemini 重新加载或更新后，旧页面脚本可能报告未捕获的“Extension context invalidated”错误。页面桥接与媒体发现现会同时处理消息调用的同步失败和异步拒绝，并安全释放运行时监听器。

## 8.9.24 — 2026-09-16

- 将 Claude 浏览器标识一致性设置改为产品说明下方的紧凑复选框，去掉重复标题与单独设置栏。这项独立偏好已经成功应用后，不再错误显示保存失败。

## 8.9.23 — 2026-09-16

- 将 Claude 浏览器标识一致性调整拆分为 Chinese Response Display Optimization for Claude 下方的独立设置。该设置不再受回复显示优化开关控制，同时继续遵循仅在本地运行且可恢复的页面运行边界。

## 8.9.22 — 2026-09-16

- 将 Chinese Response Display Optimization for Claude 扩展至受支持的 Claude 与 Anthropic 网站，并为在美国使用 Claude 的场景加入本地浏览器标识一致性调整。原有回复显示调整仍仅作用于 Claude 聊天回复，两部分由同一个产品开关管理，并遵循可恢复的页面运行周期。

## 8.9.21 — 2026-09-16

- 将控制窗口中的 XHS Image Dark Mode 动态按钮拆分为三个实时状态。功能总开关开启后，图标即显示为蓝色；开启 Always on 或检测到页面深色模式、允许功能运行后，按钮显示蓝色背景；只有页面上至少仍有一张图片处于本功能转换后的状态时，按钮才显示实线外边框。全部图片恢复原样或功能停止后，实线外边框会随即消失。

## 8.9.20 — 2026-09-16

- 控制窗口动态 C 按钮的实线外边框现会跟随页面中的实时介入状态。只有当前页面仍存在由本功能转换的 Claude 回复文本时才会显示实线外边框；相关文本在页面内跳转时被替换或移除，以及功能关闭后，外边框都会撤销。配置重新同步时会再次确认状态，同时避免反复扫描整段对话。

## 8.9.19 — 2026-09-16

- 只要控制窗口对应的标签页为精确的 `claude.ai` 域名，就始终显示动态 C 按钮，不再受功能已保存的开关状态或短暂的产品状态读取结果影响。
- Claude 的中文回复中，相邻英文字母或数字片段内部的半角标点现会保持原样。因此，英文地址不会被改用全角标点，其外围的中文行文仍会正常使用全角标点。

## 8.9.18 — 2026-09-16

- 在 `claude.ai` 页面为控制窗口加入以 C 为主体的动态按钮。蓝色背景表示功能已经开启，只有本功能确实修改了当前页面的回复文本后，按钮才会显示蓝色实线外边框。

## 8.9.17 — 2026-09-16

- 将 Chinese Punctuation Marks Display Optimization for Claude 更名为 Chinese Response Display Optimization for Claude，并将内部技术标识由 `chinese-punctuation-claude` 更换为 `chinese-response-claude`。
- 在同一功能中加入中文与相邻英文字母、数字及相关符号之间的一致空格，不另外设置开关。相关符号包括 `$45` 等货币写法、百分比、温度、提及、标签和常见运算符，普通行文标点不会被当作间距符号。可逆文本转换会自动跟随流式回复，并与标点转换共同跳过代码、链接、公式、控件和可编辑内容。
- 保留电话号码中的半角括号。这类括号属于号码格式，不作为中文行文标点转换。
- 将流式文本监听范围限制在 Claude 回复容器内，降低页面运行负担。页面级轻量监听只负责发现新增回复，不再监听无关区域的文字变化；本功能自身已经记录的文本写入也会在再次进入处理队列前直接忽略。

## 8.9.16 — 2026-09-16

- 修复 Page Display 无法运行的页面中，Reduce White Point 与 Greyscale 仍可操作并显示普通中性色的问题。无论已保存的 Page Display 设置如何，这两个控制按钮现在都会与其他功能一样显示不可用状态及对应提示。

## 8.9.15 — 2026-09-16

- 将 Chinese Punctuation Marks Display Optimization for Claude 移至 Satellites 的最后一项。设置页首次显示及页面内导航现在会保持相同顺序。

## 8.9.13 — 2026-09-16

- 新增 Chinese Punctuation Marks Display Optimization for Claude，作为 Operations Province 管理且默认关闭的 Satellite。它仅在 `claude.ai` 精确域名中替换已显示中文回复里的半角标点，并跳过代码、链接、公式、控件和可编辑文本。
- 文本处理采用页面内的增量方式并完全在本地完成。功能关闭后会恢复由它修改的文本，但不会覆盖 Claude 已经更新的内容。

## 8.9.12 — 2026-09-16

- 修复网页通过其他对象或回调调用 `window.scroll`、`scrollTo`、`scrollBy` 时，Native Scroll 可能引发 `Illegal invocation` 报错的问题。Window 滚动方法的包装层现会保留其所属 Window 作为接收对象，Element 滚动方法仍使用实际的元素接收对象。

## 8.9.11 — 2026-09-16

- 修复 No Autoplay 可能遗漏在页面配置生效前已经开始播放的媒体，包括启动后移除 `autoplay` 属性的播放器。首次运行时的一次性检查现会同时确认媒体是否正在播放，不增加轮询或持续扫描 DOM。
- 将顶层页面的 No Autoplay 决策应用到其 HTTP(S) 子框架。嵌入式播放器现与所在页面共用标准模式、强力模式、停用、白名单及自动播放音频规则。
- 仅将对媒体元素本身，或明确标识为播放、继续播放、解除静音的控件所进行的操作视为播放意图。普通点击、一般页面控件及 Chrome 的通用用户激活状态不再放行延迟启动的 HTML 媒体或 Web Audio 自动播放。

## 8.9.9 — 2026-09-14

- 扩展 XHS Image Dark Mode 的文字卡片识别能力，现可处理包含局部荧光标注和小型插图的白底文字图片。分类器会先将面积受限的明亮标注作为附属阅读表面，再分析其余前景结构；白底产品照片及其他包含大面积视觉主体的图片仍会保持原样。

## 8.9.8 — 2026-09-14

- 扩展 XHS Image Dark Mode 的文字卡片识别能力，现可处理采用鲜艳纯色背景的文字图片。分类器会同时检查占主导且稳定的背景、对比明显的文字结构以及多个彼此分离的前景区域，色彩鲜艳的照片仍会保持原样。

## 8.9.7 — 2026-09-14

- 修复小红书 CDN 的低清图片变体被判定为照片后，阻止同一图片的高清版本重新分析的问题。否定结果现在仅适用于当前图片变体，已经确认的文字卡片结果仍可在不同变体之间复用。带有轻微纹理、文字对比明显的浅色卡片会在高清图片载入后得到处理。

## 8.9.6 — 2026-09-09

- 修复 Native Scroll 在采用严格样式安全策略的网站上反复产生 Content Security Policy 内容安全策略报错的问题。标准模式和强力模式的页面规则现由 Chrome 以扩展样式注入，不再向网页写入内联样式。切换模式或停用 Native Scroll 时会完整移除相应规则。

## 8.9.5 — 2026-09-08

- 在 X / Twitter 中开启 Video Download 后，页面视频会显示下载箭头，点击即可打开所选视频的媒体菜单。帖子详情页默认列出该帖子自己的所有视频，并展示缩略图、可用清晰度和来源提供的时长。其他页面会提示先选择视频，切换帖子后会清除上一帖子的结果。
- Video Download 的所有清晰度选项均可在括号中显示已知文件大小。视频文件可通过响应头查询大小，无需预先下载媒体内容，也不会将视频流清单的大小误当成视频文件大小。

## 8.9.3 — 2026-09-06

- 修复标签页的延迟载入事件在新文档已经上报正在处理后仍会清除 XHS Image Dark Mode 状态的问题。新文档现在依照自身的文档标识接管并重置状态，控制窗口不会在功能已经生效时退回“正在等待页面深色模式”。

## 8.9.2 — 2026-09-06

- 调整 XHS Image Dark Mode 的说明与运行规则，不再按特定的主题来源区分，而是依据页面最终呈现的深色效果运行。自动处理会识别网站自身或其他页面样式提供的兼容深色信号，“始终启用”仍可跳过检测。

## 8.9.1 — 2026-09-06

- XHS Image Dark Mode 现可处理小红书用户主页中的帖子封面，包括布局尺寸尚未建立时载入的封面。用户主页中的帖子封面也不会再被误判为头像。
- 补充识别小红书自身的页面深色标记，并在页面内跳转时保留已经检测到的状态。页面状态现在与当前文档绑定并按顺序写入，避免较早的结果让控制窗口错误显示“尚未检测到页面深色模式”。

## 8.8.5 — 2026-09-06

- 重新设计 XHS Image Dark Mode 的控制窗口图标，将翻开的书放在上半部，灯泡放在下方。等待状态仍使用空心灯泡，生效状态仍使用实心灯泡。

## 8.8.3 — 2026-09-06

- 简化 Page Display 设置页的层级。Reduce White Point 与 Greyscale 的开关现在直接对齐在各自小标题右侧，只有亮度降低幅度保留为下一级设置。

## 8.8.2 — 2026-09-06

- 调整控制窗口中的功能顺序。Image Download 与 Video Download 现在位于 Page Display 控制项上方，Reduce White Point 与 Greyscale 移至第五行。

## 8.8.1 — 2026-09-06

- 在 Any Copy 下方新增控制窗口第四行，放置 Reduce White Point 与 Greyscale 的全局开关。即使当前标签页不允许插件注入页面代码，两个按钮仍可直接修改各自保存的全局设置。开启后，按钮会显示蓝色图标与背景色。
- Reduce White Point 沿用原有的灯泡图案，Greyscale 使用左侧实心、右侧空心的圆形图案。XHS Image Dark Mode 改用翻开的书与灯泡相结合的新图标，同时保留等待状态下灯泡空心、处理状态下灯泡实心的区别。
- Page Display 已从 Satellites 移至独立设置页面。设置导航会在 Satellites 左侧显示亮度太阳图标，两项显示调整也分别配有自己的功能图标。
- Page Display 新增总开关。关闭后，受影响的网页会恢复原样，全部子选项会灰度且不可操作，但已经保存的选择仍会保留。必要时，从控制窗口开启任一显示调整也会同时开启总开关。

## 8.7.1 — 2026-09-06

- 修复小红书瀑布流不断增长后，XHS Image Dark Mode 持续累积图片观察、尺寸观察、按钮定位和连续分析任务的问题。普通照片完成判定后会立即退出观察，需要保留还原能力的已转换图片则只保留必要状态。
- 尺寸观察和滚动时的按钮定位现在仅用于实际带有控制按钮的详情图片。后台分析会在图片之间把执行机会交还给浏览器，当前画面和详情图片仍会优先处理，原有的前向处理范围保持不变。
- 更换详情查看器、复用图片元素、移除网页节点或重新启动功能时，会及时清理失效的按钮、监听器、记录与排队任务。上一轮生命周期开始的分析无法再修改新一轮状态。

## 8.6.1 — 2026-09-05

- 修复页面深色模式通过根级反转滤镜实现时，Reduce White Point 反而使部分页面变亮的问题。Page Display 现在会根据实际的上层滤镜方向调整合成颜色，确保最终画面的亮度只会保持或降低，图片、视频、Canvas 等渲染内容也一并适用。
- 页面主题或全屏状态变化后会通过限定范围的事件和观察机制同步调整，不进行轮询，也不读取网页内容。

## 8.5.1 — 2026-09-05

- 本次更新仅调整 Ad Marshal 在 `news.qq.com` 上的正文音频和视频规则。在该网站中，Ad Marshal 现在会独立于 No Autoplay 阻止自动播放，保留正文播放器封面，并在用户操作播放器后才加载媒体。用户首次播放视频时仍保持静音，之后主动解除静音则会予以保留。
- 本次更新仅调整 Ad Marshal 在 `news.qq.com` 上的正文视频悬浮窗规则，将原先的删除处理改为暂停。正文播放器进入悬浮状态后会被隐藏、暂停并恢复为 `preload="none"`，但播放器节点和媒体来源均会保留，方便用户回到正文位置后继续使用。Ad Marshal 仍会直接删除 `news.qq.com` 中已识别的右侧视频模块。

## 8.3.7 — 2026-09-05

- 将 `news.qq.com` 的视频模块由隐藏改为直接删除。Ad Marshal 现在会先暂停并释放媒体，再删除 `.qqcom-jxvideo`、`.video-wrap` 和腾讯视频框架。右侧非视频内容栏仍通过 CSS 隐藏。

## 8.3.6 — 2026-09-05

- 在 `news.qq.com` 的 Ad Marshal 规则中新增腾讯新闻视频悬浮窗拦截。播放器进入悬浮状态后，Ad Marshal 会暂停播放、释放媒体来源并删除悬浮节点。普通内嵌播放器在进入悬浮状态前不会被处理。

## 8.3.5 — 2026-09-05

- 扩展 `news.qq.com` 的 Ad Marshal 规则，隐藏右侧内容栏、嵌入式视频模块、视频容器和腾讯视频框架，同时不增加 DOM 观察器或轮询。关闭该规则后，运行代码与请求规则会立即停止；刷新网页即可恢复当前页面中已经隐藏的区域。

## 8.3.3 — 2026-09-05

- 移除 Image Download 界面中的“推荐版本”角标与文字。插件仍会默认选择最佳可用来源，并保留其他尺寸和格式供用户选择。
- 点击 Image Download 的停止按钮后，界面会立即结束扫描提示，再优先沿用 Chrome 的原生关闭过渡退出 Side Panel。较旧版本也会先显示简短的退出过渡，避免侧栏突然消失。
- 修复重置时可能未停止后续标签页中 Image Download 会话的问题。现在所有活动会话都会完成清理。

## 8.3.2 — 2026-09-05

- 将 Image Download 与 Video Download 的停止扫描图标改为红色，并保持按钮无背景色。

## 8.3.1 — 2026-09-05

- 修复以数字开头的域名（例如 `*.163.com`）被网站规则误判为无效的问题，并统一各规则编辑器对国际化域名的处理。
- 防止延迟返回的设置数据覆盖较新的选择。设置保存成功后，即使后续刷新失败，也会保留已保存的结果。同一个控件在操作完成前不会重复提交。
- 刷新时保留内容未变的网站规则控件，避免下拉菜单突然关闭。修改失败时恢复已保存的选择，总开关关闭时，其下的选项也不会被错误地重新启用。
- 为设置修改、语言切换和重置失败添加明确提示。控制窗口不再用当前网页的运行状态替代设置预览中的保存偏好，无痕环境中的选择也不会写入普通窗口的预览。
- 重置时直接用默认值替换已保存的偏好，避免替换失败时原有偏好已经被删除。关闭 Any Copy 强力模式后，即使活动状态更新失败，也不会将已完成的关闭操作误报为失败。
- 防止延迟返回的控制窗口数据和图片扫描结果恢复过时的页面控件。停止 Image Download 后，列表会立即反映停止状态，不依赖后续刷新是否成功。
- 防止延迟重连将已退出的 Video Download 列表或已隐藏的 Image Download 列表重新标记为可见，意外延长扫描。过时的语言读取和无痕环境中的重置也不会再改动普通窗口的设置预览缓存。

## 8.2.3 — 2026-09-05

- 修复从“全部设置”或其他设置页面切换到 Native Scroll、No Autoplay 后，共用白名单无法添加或删除规则的问题。无论最初打开的是哪个设置页面，`*.douyin.com` 等有效通配规则现在都能正常保存。
- 网站规则表单现在会区分格式错误与保存失败，不再将保存失败误报为域名格式无效。保存失败时，已输入的规则会保留，方便重试。

## 8.2.2 — 2026-09-05

- 在设置页面中将 XHS Image Dark Mode 标记为测试版，并新增单独的实验性功能提示。控制窗口中的名称保持不变。
- 从 Ad Marshal 中移除抖音与 Gmail。现在可选的管制网站仅包括腾讯新闻和知乎，旧设置中已经停用的选择也无法重新启用这些功能。
- 当前扩展代码不再保留旧版工具栏活动图标。浏览器工具栏会始终显示标准 Cosmic Gemini 标志，现有的标题状态与控制窗口仍会显示功能活动情况。

## 8.2.1 — 2026-09-04

- Image Download 与 Video Download 的响应监听现仅注册在正在收集媒体来源的具体标签页。未启动过这两个功能的标签页不会注册媒体 `webRequest` 监听。开始扫描时按需注册，进入两分钟等待期后继续保留，并在收集暂停或结束时移除。
- 合并 Customs Province 的并发会话恢复，并取消恢复状态不确定时覆盖全部标签页的兜底监听，避免资源密集但与下载无关的页面进入下载响应路径。
- Ad Marshal 不再使用统一开关，改为紧凑的普通网站复选框。各组网站均默认不选，并只会单独授权相应的网站规则。旧的统一开关设置不会被恢复。
- 收紧 Page Display 中 Greyscale 下方的版面间距，移除多余留白。

## 8.1.1 — 2026-09-04

- 在 Satellites 的第二项加入 Page Display。这个由 Operations Province 管理的新产品吸收了 Reduce White Point，并新增相互独立的 Greyscale。两个功能均默认关闭。
- 两项显示功能共用一层不接管鼠标操作的轻量渲染层，可覆盖网页内容、图片、Canvas、动画、嵌入式框架和视频。功能无需扫描这些元素，也不会改变布局或输入操作。
- 两项视觉效果均可单独撤销。关闭其中一项时，只会移除对应效果。两项均关闭后，页面会恢复原有显示，同时完整移除渲染层、全屏监听器、桥接和运行代码。
- 为新的产品结构补充完整的英文与简体中文设置、同步首屏控件、说明文档、隐私文案和生命周期检查。

## 7.7.1 — 2026-09-04

- 在 Satellites 的第二项加入 Reduce White Point。功能默认关闭，可在 10% 至 80% 之间调整降低程度，初始值为 25%，用于减轻网页明亮颜色带来的视觉刺激。
- 将 Reduce White Point 实现为 Operations Province 管理的独立产品。只有用户开启功能后，Central 才会载入一层不接管鼠标操作的视觉调整层。该图层覆盖页面显示，但不会改变布局或输入操作，并会跟随全屏状态调整。关闭功能后，相关图层与运行代码会被完整移除。功能不会读取网页内容，也不使用观察器、轮询、网页 API 补丁或网络请求。
- 为新功能补充同步的英文与简体中文设置、首屏偏好显示、说明文档、隐私信息和生命周期检查。

## 7.6.1 — 2026-09-04

- 移除 Ad Marshal 在抖音页面中重复运行的主页面请求补丁。已确认的遥测加载器和域名仍由 Chrome 原生且仅限当前标签页的 DNR 规则处理，Fetch、XHR、Beacon、图片加载、视频播放和预加载则保持网页原生行为。这样可以减少抖音高频瀑布流与播放器链路中的逐请求 JavaScript 工作，同时不会扩大或削弱已有遥测匹配范围。

## 7.5.2 — 2026-09-04

- 修复小红书瀑布流越往后浏览，XHS Image Dark Mode 越容易停止处理的问题。尚未载入的懒加载图片只会等待载入事件，不再占用两个图片分析任务；图片进入可视范围后，也能在待处理队列中获得更高优先级。功能仍会提前准备视口下方的图片，但未开始加载的资源不会再阻塞后续内容。详情图集还会在 `currentSrc` 更新前识别 `src` 与 `srcset` 的变化，查看器重复使用同一图片元素时也能稳定安排新页面的分析，不会继续沿用上一页的浅色状态。

## 7.5.1 — 2026-09-04

- 小红书图集中的每张图片现在都会独立识别。瀑布流封面保持原样时，不会再阻止详情首图或后续图片接受分析。只有已经确定需要深色处理的结果才会在相关页面元素之间复用，保持原样的判断仅适用于完全相同的图片资源。
- 新增对大面积稳定浅色与深色区域组合而成的文字版式的识别，包括由浅色正文区围绕深色标题区的排版。识别程序会判断连续的版面结构，不依赖固定颜色，也不会把普通文字行误认为大块面板。均匀灰底卡片在识别后也会立即自动启用黑底处理。

## 7.3.3 — 2026-09-04

- 修复展开图片时，明亮的占位背景会在高清图出现前闪烁的问题。瀑布流封面与详情首图会通过笔记标识共享识别结果，查看器也会对包含占位图和高清图的整张幻灯片统一处理。深色图片不再额外降低默认亮度，同时保留可供后续调整的内部亮度参数。

## 7.3.2 — 2026-09-04

- 为浅色文字配合均匀中灰色背景的卡片增加单独的高对比度处理，将灰底压暗为黑色，同时保留浅色文字和小面积彩色标记。

## 7.3.1 — 2026-09-04

- 修复小红书未以 CORS 模式加载图片时，图片会被静默跳过的问题。识别程序现在会改用同一 CDN 图片的 CORS 副本重新取样，并尽可能复用浏览器缓存，不会修改或重新加载页面中显示的原图。
- 扩展整图识别能力，可以识别由稳定浅色内容区和彩色边框组成的文字卡片。边框判断不依赖具体颜色，粉色、蓝色、绿色、黄色、深色及其他颜色的边框均可作为卡片结构处理，同时仍会排除照片和图文混合图片。
- 展开笔记后，每张图片都可以单独选择浅色或深色显示。切换按钮只会在图片查看器中创建，有页码时会显示在当前页码左侧，瀑布流封面不会加载或显示按钮。
- 将小红书同一原图的不同 CDN 清晰度版本归并为同一个图片标识。若瀑布流中的图片已经完成识别，展开笔记并创建高清图片元素时，会在同一轮页面更新中直接应用缓存结果，不会在等待重新分析时短暂闪回白色。

## 7.2.1 — 2026-09-04

- 将 XHS Image Dark Reader 更名为 XHS Image Dark Mode，并同步更新插件界面、源代码、设置页面和项目文档。功能简介会先说明具体作用，并明确主要用于改善 Dark Reader 页面深色模式下的小红书图片显示。
- 以更轻量的整图识别取代局部遮罩和人脸检测。分析时会保留原图比例，采样不超过 1,024 个像素，只识别具有近似单一浅色背景和适量文字前景的图片，照片及图文混合图片会保持原样。
- 每张已调整的图片现在都能单独切换浅色或深色显示，控制按钮会根据当前显示方式改变操作和说明。
- 页面深色模式检测现在会优先判断当前视口是否已实际呈现为深色，并可识别作用于整页的反色滤镜及 Dark Reader 的动态、滤镜与静态引擎。插件还会观察主题样式和页面元数据，并在页面刚载入时进行数次短暂复查。
- 开启“始终启用”后，插件会立即开始处理图片，控制窗口也会直接显示带背景的实心蓝色功能图标，无需等待页面状态同步。
- 重新整理 Mailto Capture 与 Ad Marshal 的功能简介，让第一句话直接说明各自执行的操作，并在设置页面和中英文 README 中分别采用自然的本地化表达。
- 移除已经停用的设置键和旧配置格式迁移逻辑，各项功能只读取当前配置字段。

## 7.1.1 — 2026-09-04

- 新增默认关闭的 XHS Image Dark Reader，由 Operations Province 管理，并列在 Satellites 第二位。访问 `www.xiaohongshu.com` 时，控制窗口会在固定功能下方显示灯泡开关，并分别呈现关闭、等待深色模式和正在处理三种状态。
- 新增完全在本地运行的低分辨率图片识别，只调整能够确定为文字卡片或文档的区域，人像、风景、普通照片、头像及无法确定的区域均保持原样。多图笔记会逐张独立处理，图文混合图片则通过局部遮罩调整，不会改变整张图片。
- 新增单张图片恢复按钮及透明度设置，并支持自动检测深色页面或手动忽略检测。只有功能获得授权并开始处理后，图片观察器、样式、遮罩和控制按钮才会运行，停止后会完整移除。
- 加宽 Satellites 各项说明与开关之间的间距，并让隐私说明采用相同的换行边界。XHS Image Dark Reader 总开关关闭后，全部子选项会以清晰、统一的中性灰色停用；隐藏图片控制按钮时，透明度选项也会一并隐藏。隐私说明删去仅用于描述开关状态的赘述，同时保留与数据处理有关的信息。
- 重新整理 Satellites 的全部中英文界面文案，包括页面简介、帮助内容、各项功能说明、隐私说明、XHS 子选项、控制窗口状态提示及全部设置中的入口。两种语言均按照各自的自然表达独立编写。
- 修复 XHS Image Dark Reader 的配置下发流程。页面正在同步旧配置时，新的开关设置不会再被遗漏。页面运行端会确认已经应用最新配置，忽略深色模式检测的设置也会在操作完成前生效。
- 深色模式检测现在会直接识别 Dark Reader 的页面标记，并结合更多主题节点与实际可见区域判断页面配色，不再依赖单一根节点的背景颜色。

## 6.12.2 — 2026-09-04

- 腾讯新闻时间线页面使用的监控脚本现在会由本地兼容实现接管，不再启动对 Fetch、XHR、Console 与性能数据的监测，同时避免页面因缺少接口而产生新错误。该页面所使用的 `n.ssp.qq.com` 广告端点也会在本地处理，正文、图片与音频资源不受影响。

## 6.12.1 — 2026-09-04

- Ad Marshal 现将腾讯新闻内部的 `view.inews.qq.com` 时间线页面纳入现有腾讯新闻管制项。该页面中已确认的上报加载器与腾讯遥测请求会沿用 `news.qq.com` 的本地处理方式，设置页面仍只显示一个腾讯新闻条目，其他 `qq.com` 子域名仍不在适用范围内。

## 6.11.1 — 2026-09-03

- Bili Daily Login 现在每天进行两次相互独立的检查。较早一次确认完成后，仍会保留当天稍后的可靠性检查。错过的日期与更早的检查时段不会补做，Chrome 重启后也不会重复已经执行的时段。请求失败时会等待下一时段，不会形成重试循环。
- 账号检查会使用 Chrome 中已有的登录状态，并仅在请求期间临时添加与哔哩哔哩一致的请求头，同时设置请求时限并明确核验每日奖励状态。整个过程不会打开哔哩哔哩网页或标签页，设置页面也只说明功能结果，不再展示具体日程。

## 6.10.7 — 2026-09-03

- 将 Mailto Capture 浮层标题调整为 14px，在保留与字段内容层级差异的同时，让紧凑的标题栏不再显得过重。

## 6.10.6 — 2026-09-03

- 将 Mailto Capture 的关闭按钮与标题文字按基线对齐，并单独调整“×”符号，使两者保持在同一视觉行上。

## 6.10.5 — 2026-09-03

- 在保持标题字号不变的情况下，缩小 Mailto Capture 标题栏的行高、顶部内边距、内容间距与关闭按钮，降低标题区域的整体高度。

## 6.10.3 — 2026-09-03

- 移除 Mailto Capture 操作按钮下方空置的状态提示行。只有复制成功或失败的提示出现后，该区域才会占用空间。

## 6.10.2 — 2026-09-03

- 缩小 Mailto Capture 浮层的宽度、最大高度、内边距、内容间距与阴影，减少对网页空间的占用。同时适当增大字段标签与字段内容的字号，标题和按钮文字大小保持不变。

## 6.10.1 — 2026-09-03

- Satellites 设置页首位新增 Mailto Capture。该功能在普通窗口中默认开启，在无痕模式中默认停用，可以单独关闭，不会在控制窗口中增加按钮或活动图标。
- Mailto Capture 会在系统邮件应用打开前拦截邮件链接，并在简洁的封闭式 Shadow DOM 浮层中显示收件人、抄送与密送地址、主题、正文和其他附带信息。链接只包含一个邮箱地址时提供“复制地址”，包含其他内容时则只提供“复制邮件内容”。点击浮层外部或按 Esc 即可关闭，插件不会保留其中的邮箱地址或邮件信息。

## 6.9.1 — 2026-09-03

- Ad Marshal 新增严格限定范围的 Gmail 管制规则。在已开启功能的 `mail.google.com` 标签页中，插件会在其他拦截器拒绝请求前，为发往 `play.google.com/log` 的遥测数据返回本地成功结果，避免日志队列不断重试。
- 该规则也会处理使用同一日志组件的内嵌 Chat 与 Google 顶栏页面，但不会阻止相关脚本，也不会影响 Gmail 的邮件数据、同步、附件、身份验证、Chat、Meet 或通知连接。

## 6.8.5 — 2026-09-02

- 深色模式下的设置页开关进一步向 Cosmic Gemini 标志蓝色靠拢，同时保留与深色背景之间的清晰对比。

## 6.8.3 — 2026-09-02

- 深色模式下的设置页开关现在使用折中的中蓝色，介于 Cosmic Gemini 标志蓝色与原有的深色主题亮蓝色之间。

## 6.8.2 — 2026-09-02

- 设置页面的开关在浅色与深色主题下均使用更深的 Cosmic Gemini 标志蓝色显示启用状态。

## 6.8.1 — 2026-09-02

- 设置页面现在会单独接收只读的用户配置，不再从混合的功能运行状态读取持久控件。网页匹配、运行活动、浏览器能力、计划任务、扫描过程和 DNR 规则同步均不会影响用户保存的选择。
- 检查 Native Scroll、No Autoplay、声音自动播放、Bili Daily Login、Ad Marshal、Image Download 和 Video Download 的持久设置，并新增回归测试，明确区分用户设置与实际运行状态。

## 6.7.6 — 2026-09-02

- 优化 Satellites 设置页中有关功能独立开启及数据处理说明的中文表述。

## 6.7.5 — 2026-09-02

- 精简 Ad Marshal 设置页中的“管制网站”中文标签。

## 6.7.3 — 2026-09-02

- Ad Marshal 的统一开关现已与网页状态及 Chrome DNR 状态完全分离。设置页面只显示用户保存的选择，管制网站匹配仅用于确定网页的 `active` 运行状态，DNR 规则同步则由产品独立处理。

## 6.7.2 — 2026-09-02

- 修复 Ad Marshal 统一开关在 Chrome 同步原生网络规则时可能表现为无法操作的问题。设置保存与规则同步现已分开，开关保存后即可响应，后续同步由产品内部继续完成。
- 设置页面的开关现在会将整个滑轨作为原生输入区域，使鼠标操作更加稳定。

## 6.7.1 — 2026-09-02

- Ad Marshal 现在使用一个统一开关管理全部管制网站，初始状态为关闭。设置页面会在一个自然段中列出管制网站，各网站仍在内部使用独立规则。旧版的逐站点设置不会开启统一开关，也不会迁移到新设置中。
- 新增适用于知乎根域名及全部子域名的内部规则。该规则会处理当前页面中确认的数据分析加载器，以及数据分析、性能监测、实验上报、崩溃报告和百度统计接口，不会匹配知乎独立的图片与静态资源域名。
- 抖音的内部规则现在也适用于其直播网站，设置页面不会单独列出该网站。

## 6.6.1 — 2026-09-02

- 腾讯新闻仍使用一个设置开关，但 `news.qq.com` 与 `www.qq.com` 现在分别采用独立的内部规则。`www.qq.com` 的规则会单独处理该网站的监控加载器、已确认的广告图片来源与广告区域，不会扩展到其他 `qq.com` 子域名。
- 调整腾讯新闻设置的说明文字，更自然地表达该设置同时适用于两个网站。

## 6.5.6 — 2026-09-02

- 腾讯新闻备用主页域名的标签现在会随界面语言显示，并重新调整“管制网站”标题字号，使其层级介于功能标题与网址标签之间。

## 6.5.5 — 2026-09-02

- 缩小 Ad Marshal 设置中“管制网站”标题的字号，使卡片内的信息层级更加清晰。

## 6.5.3 — 2026-09-02

- 腾讯新闻的 Ad Marshal 规则现在同时适用于 `news.qq.com` 和另一个主页域名 `www.qq.com`。匹配范围仍限定为这两个域名，不会扩展到其他 `qq.com` 子域名。

## 6.5.2 — 2026-09-02

- 现在从 Chrome 打开扩展程序选项时会进入“全部设置”，不再直接进入 Native Scroll 设置页。

## 6.5.1 — 2026-09-02

- Ad Marshal 新增独立的 `douyin.com` 管制规则，初始状态为关闭。该规则会处理已经确认的 AppLog 数据采集加载器、Slardar 浏览器监控加载器和限定范围内的字节跳动遥测域名，不会匹配视频播放、内容推荐、账号登录或安全验证。
- 插件现在会记录每个管制标签页实际使用的网站规则。当标签页在不同管制网站之间跳转时，会完整替换该标签页的临时 DNR 规则。抖音规则不扫描网页内容，不使用观察器、计时器或轮询，停用后会释放全部临时规则和网页 API 补丁。
- 已有的 Ad Marshal 网站选择会在插件更新和重新载入后继续保留。Satellites 会分别列出腾讯新闻与抖音，且只保存这两个网站的开关设置。

## 6.3.1 — 2026-09-01

- Ad Marshal 现在会在插件内部处理腾讯新闻的广告接口及 `127.0.0.1:11601/check` 本地客户端探测，避免网页连接本机回环地址，也不会再因连接失败而发起相关上报。
- 已知的广告与上报加载器现在会收到本地空脚本，不再得到失败响应。跟踪图片也会在进入网络前被替换，减少资源加载失败后产生的补充报告。
- 新增在腾讯新闻文章页中确认的广告与上报端点，同时继续保持无需页面观察器的轻量运行方式，所有管制网站仍默认关闭。

## 6.2.1 — 2026-09-01

- Ad Marshal 在普通窗口和无痕模式中均默认关闭。只有用户开启某个管制网站的开关后，Ad Marshal 才会在该网站运行。
- 修复 Ad Marshal 删除仍由 React 管理的节点后，腾讯新闻显示为全黑页面的问题。Ad Marshal 不再删除网页节点，也不再修改 Beacon 元数据，让网页应用可以正常完成渲染与后续更新。
- 已确认的广告与上报加载器会在运行前被阻止，对应的跟踪请求会收到与资源类型相符的本地响应，已知广告区域则通过窄范围本地样式隐藏。页面运行代码不再需要 DOM 观察器或轮询。

## 6.1.1 — 2026-09-01

- Satellites 新增 Ad Marshal，并为每个管制网站提供独立开关。
- Ad Marshal 会阻止已确认的腾讯新闻广告与上报加载器运行，即使脚本来自外部腾讯 CDN 也能识别。对应的上报调用会收到本地成功结果，已知广告元素会被移除，正常内容中的 Beacon 上报标记也会被清除，让网页无法继续形成失败与重试的循环。
- Standing Province 依照 Central 的决策协调 Ad Marshal。标签页专用的轻量网络规则、窄范围页面运行代码，以及离开网站或关闭开关后的完整清理由 Ad Marshal 独立负责。

## 5.17.1 — 2026-09-01

- Video Download 现在可以区分同一网页中的多个视频。每个视频会分别显示为一张卡片，并在可用时显示对应的缩略图或标题。清晰度、音频和字幕选项会归入各自的视频。
- DOM 播放器、嵌入式页面、兼容的网站适配器及展开后的 HLS 或 DASH 视频流现在会在查找和格式处理过程中保留视频归属。暂时无法判断归属的视频流也会单独保留，不再因为清晰度相同而被静默合并。

## 5.16.1 — 2026-09-01

- Image Download 现在会先从网页中的直接图片来源及资源记录快速取得首批结果，再继续检查其他可用来源。普通扫描以当前主页面为主，完整扫描仍会继续处理子框架、懒加载内容、CSS 背景、SVG、Canvas 与 Shadow DOM。
- 工作区会分别显示启动、查找图片和检查来源等阶段，并提供加载动画、进度条及实时结果数量。扫描超过时限后会进入可恢复状态，不再让刷新按钮一直处于不可用状态。
- 同一标签页中的重复扫描会合并处理。大量图片卡片会分批显示，预览尺寸也会合并写入，减少重复存储和整个图片列表的反复重绘。

## 5.15.5 — 2026-09-01

- 修复首次打开 Image Download 侧边栏时，侧边栏与原标签页会话之间偶尔不同步的问题。工作区现在会短暂等待会话建立，并保留隐藏期间收到的状态更新。等待时会显示本地化的启动提示，不再出现内容为空且操作按钮不可用的界面。

## 5.15.3 — 2026-09-01

- 修复 Image Download 按钮因当前标签页校验占用了 Chrome 的用户操作时机而无法打开侧边栏的问题。现在，侧边栏会在用户点击后立即开始打开，同时继续校验当前标签页。

## 5.15.2 — 2026-09-01

- Image Download 与 Video Download 现在会在控制窗口中显示资源查找状态。正在扫描时，图标为蓝色并带有背景；进入两分钟等待期后，图标仍为蓝色，但不再显示背景；扫描暂停后，图标恢复为中性色。

## 5.15.1 — 2026-09-01

- 修复 Native Scroll 在同一页面中停用后重新启用时可能无法恢复保护的问题。停用期间，页面内只会保留此前已识别的滚动劫持监听器的非活动弱引用信息，不会运行任何挂钩、观察器或拦截器。只有用户要求重新启用，并由 Central 批准再次启动后，新载入的运行代码才会读取这些信息。

## 5.13.1 — 2026-09-01

- Native Scroll 现在会从页面初始化开始保留小红书原有的页面接口和布局，使信息流中的帖子卡片可以正常展开，展开后的图片切换也不受影响。其他网站的保护逻辑保持不变。

## 5.12.1 — 2026-09-01

- 无痕窗口现在使用独立的内存配置环境。每次开启新的无痕会话时，所有功能均处于停用状态，也不会继承普通窗口中保存的设置、网站规则或界面语言。
- 用户在无痕窗口中明确作出的选择仅在本次无痕会话中有效。关闭最后一个无痕窗口后，这些临时设置会被删除。在无痕设置页执行重置也不会修改普通窗口的设置。
- Native Scroll 与 No Autoplay 的设置页会注明功能在无痕模式下默认停用。Bili Daily Login 的开关会替换为“已在无痕模式下停用”，无痕后台无法为其建立计划。

## 5.11.1 — 2026-09-01

- 继续扩大可靠性检查范围，覆盖共享设置、普通与无痕后台环境、定时任务、界面首次载入及媒体下载交接。
- 在一个 Chrome 环境中修改设置后，另一个环境中的相关页面功能、已打开的设置页和控制窗口会及时同步。仅用于下载的偏好修改不会触发无关网页重新同步。
- Bili Daily Login 现在仅由普通浏览环境负责调度。插件会清除旧版本可能留在无痕环境中的计划，并且只在普通 Chrome 窗口存在时发出请求。
- 控制窗口与 Image Download 工作区会在首次状态读取完成后再显示。Service Worker 暂时不可用时，功能按钮会保持停用，不会在缺少当前状态时接受操作。
- Image Download 与 Video Download 会在离屏请求释放保活之前接管刚生成的本地文件。Chrome 正在接收下载时，即使来源标签页关闭或会话停止，也不会再提前撤销文件。

## 5.10.1 — 2026-09-01

- 继续扩大可靠性检查范围，覆盖全部重置、界面关闭、媒体下载交接、离屏处理、Service Worker 生命周期及无痕模式分离运行。
- 重置插件时会保留 Chrome 已接受下载仍需使用的临时文件记录。可选的离屏回收失败不再让已完成的操作显示为错误，停止 Image Download 并清理截图文件后也会释放空闲的本地处理器。
- Image Download 与 Video Download 会在 Chrome 接受下载后立即接管临时文件，并重新核对在会话信息保存前已经结束的下载。后续状态写入失败不再撤回 Chrome 正在下载的文件，快速完成的格式也不会一直停留在下载中。
- 用户主动发起离屏媒体处理后，Service Worker 只会在本次处理任务执行期间保持可用。普通窗口与无痕窗口分别使用独立的临时请求规则编号，任一环境都不会再移除另一环境的媒体请求头规则。
- 控制窗口和 Image Download 工作区开始关闭后，不会再重新连接或刷新。Image Download 工作区缺少有效来源标签页时也不会再将其误认为标签页 0。
- 修改界面语言后，正在工作的工具栏标题会立即更新。语言读取或工具栏刷新暂时失败时，已经保存成功的状态修改不会再显示为错误。

## 5.9.1 — 2026-09-01

- 继续扩大生命周期检查范围，覆盖 Service Worker 恢复、网页脚本叠加、媒体任务取消、Chrome 下载衔接、区域截图及响应监听恢复。
- Service Worker 重启后，Video Download 会把中断在处理阶段的格式恢复为可重试状态。停止功能、关闭来源标签页或重置插件时，仍在本地组装的任务会被取消，已经交给 Chrome 的下载则不受影响。
- 资源查找结束后，Video Download 的扫描器与页面钩子会一并释放当前页面中的状态。网页在插件外层再次包装相关方法时，插件停用后被保留的旧包装也不会继续处理页面。临时请求头规则现在使用独立且有明确范围的编号区间。
- Image Download 在同一页面只会保留一层区域截图界面，会话结束时也会主动关闭。重新开始截图不再遗留上一层界面的键盘监听。
- 页面桥接会忽略功能释放后才返回的旧配置。Customs Province 恢复运行时还会重新核对响应监听状态，不再为已经停止收集资源的会话继续保留监听器。
- Chrome 已接受下载后，即使临时会话存储一时无法记录交接信息，本地媒体文件仍会保留到下载结束。新增针对处理中断、临时文件保留、网页方法叠加及运行代码完整释放的回归检查。

## 5.8.1 — 2026-09-01

- 进一步检查中央路由、页面运行代码、控制窗口操作、当前标签页工具、定时任务、区域截图及插件界面连接的生命周期与恢复流程。
- 中央现在会核对网页事件、插件命令、处理进度和界面长连接的来源。页面运行代码只会处理发起请求的那一份 Chrome 页面文档，载入中途失败时也会先完整撤回已经载入的部分。某项功能从未在页面中启动时，也不会再为它载入清理代码。
- 修改当前网站设置前，插件会确认原标签页尚未跳转。Image Download 与 Video Download 启动时会重新读取当前标签页，不再沿用控制窗口中可能已经过时的页面信息。
- Any Copy 强力模式会依次处理短时间内连续发出的标签页操作。关闭 Bili Daily Login 会中止正在进行的请求，旧任务无法重新建立调度，修改这一偏好时也不会再刷新无关网页。
- Image Download 会在可见来源标签页发生变化时取消区域截图，清理未完成的截图文件，并在同源单页应用跳转后重新查找图片。图片和视频在资源密集页面中的短时响应队列也增加了数量上限。
- 新增针对消息与连接来源、限定页面文档的运行代码撤回、标签页并发操作、过时控制窗口操作及定时任务中止的回归检查。

## 5.7.1 — 2026-09-01

- 扩大 Image Download 与 Video Download 的恢复及生命周期检查范围，覆盖网页传入的数据、临时会话存储、扫描器载入、离屏处理和交由 Chrome 下载后的衔接过程。
- Video Download 会在两分钟的资源查找时间结束后移除页面和插件消息监听。扫描器只完成部分载入时，插件会同时撤回两个页面环境中的运行代码，不再留下单独运行的部分。
- 限制媒体网址、清单消息、候选资源集合及自动展开清单的规模。Chrome 临时存储空间不足时，插件会逐步减少未使用的查找结果，同时保留本地截取内容及正在处理的下载。
- 临时媒体文件会在 Chrome 接受的下载完成前持续可用。图片截取失败时会删除未完成文件，本地处理器下次启动时也会清理超过七天且没有任何会话引用的文件。
- 新增针对超大媒体输入、会话数据上限、下载临时文件保留和扫描器完整卸载的回归检查。

## 5.6.1 — 2026-09-01

- 修复 Any Copy 网站规则无法正常移除的问题。Service Worker 暂时无法读取设置时，界面也会继续使用用户已经选择的语言。
- 单项页面功能同步失败后，插件会重新同步整组页面功能。页面跳转前遗留的事件不会再影响新网站，刷新标签页配置时也会进行有限次数的恢复尝试。
- 媒体会话现在能够区分 Chrome 暂时无法查询下载状态与下载已经结束这两种情况。已完成下载的清理操作会进行有限重试，临时文件标识在离屏文档重启后仍可用于恢复和清理。
- 离屏文档的建立与关闭现在依次执行，正在处理的媒体任务不会失去处理器。扫描计时器建立失败时会安全停止查找，重置后残留的扫描器也会被关闭。Bili Daily Login 会在有限次数内修复遗漏的调度。
- 修复 Image Download 区域截取的取消操作、可重试的图片信息回传及 Video Download 扫描器回执。自动检查现已覆盖双语文案一致性及本轮新增的恢复边界。

## 5.5.1 — 2026-09-01

- 各标签页的活动状态与下载会话现在会依次更新，多个功能事件同时发生时不会再互相覆盖，也不会遗漏新发现的媒体。
- 设置、界面语言与全部重置共用可恢复的写入顺序。重置完成后，较早发出的未完成写入不会再把旧设置写回来。
- 单项功能的状态读取异常不再影响整个控制窗口或设置页。页面桥接也能从短暂的 Service Worker 通信异常中恢复，已经成功应用的页面设置不会因回执失败而被撤销。
- 页面跳转前产生的扫描结果不会写入新页面。Service Worker 恢复时还会清理失联标签页的会话、活动状态、临时请求头规则及已完成媒体留下的临时文件。
- 已经交给 Chrome 的下载不再依赖媒体工作区继续开启。同一媒体地址的并发下载会分别保留文件名，Bili Daily Login 的偏好保存也与有限次数的调度修复分开处理。

## 5.3.3 — 2026-09-01

- 控制窗口和 Image Download 工作区现在会分别处理操作结果与后续状态刷新。短暂的状态读取异常不会再让已完成的操作显示为失败。
- 设置页、控制窗口和 Image Download 工作区的只读状态刷新新增有限重试。设置写入及用户操作均不会被自动重复提交。

## 5.3.2 — 2026-09-01

- 修复共用白名单偶尔提示更新失败的问题。设置写入成功后，刷新已打开页面或重新读取设置界面的临时异常不会再被误报为保存失败。
- 单次设置写入失败后，串行写入队列现在可以自行恢复，后续操作无需等待 Service Worker 重启。

## 5.3.1 — 2026-09-01

- Native Scroll 现在会保留小红书放大笔记中的滚轮切图操作，同时继续保护页面其他区域的原生滚动。
- 使用 Native Scroll 强力模式时，插件也不会再改写放大笔记阅读器所需的页面结构。

## 5.2.1 — 2026-09-01

- Image Download 与 Video Download 现在共用一项按会话状态管理的浏览器响应监听器。只要仍有下载会话正在查找资源，或处于两分钟宽限期内，监听就会保持启用。最后一个会话暂停或结束后，监听器会随即释放。
- 重新打开任一下载工作区时，插件会先恢复响应监听，再继续扫描。多个产品和标签页由 Customs Province 统一协调，产品之间仍保持独立。
- Service Worker 启动时会先恢复已有下载会话，再判断是否可以停止监听。若恢复结果不确定，监听会继续保留，以免有效会话遗漏资源。

## 5.1.1 — 2026-08-31

- 将插件后台重构为 `central → province → product → feature` 四级架构。中央层只负责判断归属并路由任务，三个省级模块负责协调各自管辖的产品，各产品则独立完成具体工作。
- Standing Province 管理 Native Scroll 与 No Autoplay。Operations Province 管理 Any Copy、Any Copy 强力模式、Satellites 和插件管理操作。Customs Province 管理 Image Download 与 Video Download。各省使用稳定的统一接口，后续调整管辖范围时无需再把职责集中回中央层。
- Image Download 与 Video Download 已拆分为相互独立的执行模块，同时由 Customs Province 协调共用的离屏文档。产品之间不能直接引用或控制彼此。
- 页面桥接、运行时、扫描器、网站适配器和离屏处理器归入功能实现层，并可根据技术需要继续拆分子功能。
- 更新架构检查、技术文档和内部维护说明，以持续约束各层的决策、协调与执行边界。

## 3.5.1 — 2026-08-31

- Native Scroll 与 No Autoplay 新增同一份共用白名单。两个设置页面均可查看和修改，白名单优先于各项功能的网站规则，移出白名单后会继续采用原先保存的设置。
- 新增 `background/central.js`，统一管理所有功能、控制窗口操作、设置命令、全部设置命令、定时任务、下载会话、浏览器标签页读取、界面偏好存储和页面运行时。`content/central-page.js` 成为唯一固定载入的页面入口，其中不包含任何具体功能行为，也不会自行监听设置存储。
- `central` 只会在对应功能于当前页面环境中启用时，分别载入 Native Scroll、No Autoplay、Any Copy 和 Any Copy 强力模式。停用的功能不会启动桥接或运行时代码。已经运行的功能被停用后，会先恢复页面改动，再释放该功能代码。
- 符合共用白名单规则的网站不会安装 Native Scroll 或 No Autoplay 的页面钩子，也不会受到观察、样式调整或媒体干预。控制窗口会停用这两项功能的按钮，用户仍可主动开启其他点击后运行的工具。
- 两个设置页面中的“网站行为”更名为“网站设置”。

## 3.1.38 — 2026-08-31

- 各项功能的独立设置页均在介绍卡片标题左侧显示对应的中性配色图标，全部设置页也采用相同设计。
- Any Copy 图标由“Any Copy 网站”区域移至顶部的 Any Copy 介绍标题。

## 3.1.37 — 2026-08-31

- 点击后启用的功能统一使用“点击时”标签，与 Chrome 的网站访问权限表述保持一致。

## 3.1.36 — 2026-08-31

- Any Copy 设置页现在明确说明，Any Copy 强力模式仅在用户点击启用的当前标签页中生效。

## 3.1.35 — 2026-08-31

- 统一 Any Copy 网站与 Any Copy 强力模式设置卡片中标题和说明文字的间距，使其与其他设置区域保持一致。

## 3.1.33 — 2026-08-31

- Native Scroll 与 No Autoplay 不再分别管理相互重叠的启用状态和运行模式，改为统一的“网站行为”设置。每条规则只会设为始终停用、始终使用标准模式或始终使用强力模式，并可直接切换。
- 现有网站设置会自动迁移到新的三种行为中。精确规则与通配规则的优先关系保持不变，即使全局默认状态为关闭，标准模式和强力模式规则仍可让指定网站保持启用。
- 两个设置页面均改用统一的规则输入区和分类列表，并将精确主机名与通配域名的说明分行显示。

## 3.1.32 — 2026-08-31

- Any Copy 强力模式由按网站启用改为当前标签页会话。该模式仅影响开启它的标签页，在该标签页内跳转时保持运行，手动关闭或关闭标签页后即会结束。
- 设置页不再提供 Any Copy 强力模式的网站规则列表，改为简明说明当前标签页行为，并停止使用旧的强力模式网站规则。

## 3.1.31 — 2026-08-31

- 移除 Native Scroll 与 No Autoplay 介绍卡片中的“默认设置”提示文字。

## 3.1.29 — 2026-08-31

- 调整 Native Scroll 与 No Autoplay 设置页中的“网站例外”说明，引导用户添加希望按照个人偏好运行的网站。

## 3.1.28 — 2026-08-31

- Image Download 与 Video Download 会在工作区打开期间持续查找资源。工作区关闭后的两分钟内仍会继续查找，随后暂停并保留已有结果。重新打开工作区后会立即恢复并重新扫描页面。
- 下载工作区改用事件驱动更新，不再定时轮询。短时间内发现的网络资源会合并处理，Video Download 也只会重新扫描发生变化的页面区域。
- 所有功能设置页与全部设置页的首张介绍卡片统一采用 Native Scroll 和 No Autoplay 使用的淡蓝色边框。

## 3.1.27 — 2026-08-30

- 英文版与简体中文版更新日志新增互相跳转的链接。

## 3.1.26 — 2026-08-30

- 移除 Bili Daily Login 介绍中单独的“Background:”标签，使 Satellites 子功能的说明直接连贯地呈现。

## 3.1.25 — 2026-08-30

- 按职责重新整理插件源码。`extension` 目录下直接存放的文件现在只有 `manifest.json`。Service Worker 位于 `background`，控制窗口位于 `popup`，共享界面与本地化模块位于 `shared`，Image Download 工作区位于 `workspaces/image-download`。同步更新 Chrome 入口、运行时网址、模块引用、检查脚本和文档。产品行为保持不变。

## 3.1.23 — 2026-08-30

- 放大 All Settings 图标及其按钮，使图标仅略小于产品图标，同时保留次要操作的视觉层级。

## 3.1.22 — 2026-08-30

- 再次放大控制窗口中的产品图标和按钮，同时适度放大 All Settings。重新调整两列间距，使窗口在现有宽度下仍保持均衡留白。

## 3.1.21 — 2026-08-30

- 同步放大控制窗口中的八个产品图标及其按钮点击区域，并重新平衡两列间距。控制窗口宽度和 All Settings 按钮保持不变。

## 3.1.19 — 2026-08-30

- 适当放大控制窗口中的八个产品图标，同时保留按钮点击区域和 All Settings 图标的原有大小。

## 3.1.18 — 2026-08-30

- 深色模式下的插件全局统一使用更明亮的蓝色，包括设置页面和下载工作区。蓝色实心按钮改用专门的深色文字以保持清晰对比，浅色模式不变。

## 3.1.17 — 2026-08-30

- 深色模式下的控制窗口改用更明亮的蓝色，使启用图标、按钮背景和活动外框更易辨认。浅色模式和插件其他页面的配色保持不变。

## 3.1.16 — 2026-08-30

- Native Scroll 或 No Autoplay 在当前页面实际执行拦截后，正在使用的模式按钮会显示蓝色细外框。控制窗口在打开期间也会及时更新提示。

## 3.1.15 — 2026-08-30

- 进一步收窄控制窗口，同时保留现有的图标尺寸、间距和按钮点击区域。

## 3.1.13 — 2026-08-30

- 适当收窄控制窗口，同时保留现有的两列布局和按钮点击区域。Video Download 媒体面板的宽度保持不变。

## 3.1.12 — 2026-08-30

- 将紧凑控制窗口重新设计为四行，每行放置两个相关按钮，分别对应 Native Scroll、No Autoplay、Any Copy 和下载功能。Cosmic Gemini 名称暂时隐藏，全部设置移动到底部的独立一行并靠右显示。

## 3.1.11 — 2026-08-30

- 修正 All Settings 中灰色图标底板的背景层级，使叠放图案在深色模式下使用底板本身的颜色，而不是外层卡片的黑色背景。Any Copy 图案本身没有改动。

## 3.1.10 — 2026-08-30

- Native Scroll 与 No Autoplay 介绍卡片中的核心说明改为与下方补充说明相同的次要文字颜色。

## 3.1.9 — 2026-08-30

- Native Scroll 与 No Autoplay 介绍卡片中的开关改为与产品名称所在行垂直对齐。

## 3.1.8 — 2026-08-30

- Native Scroll 与 No Autoplay 的介绍文案改用旧版简洁直接的核心说明，现有卡片设计和设置流程保持不变。

## 3.1.7 — 2026-08-30

- Any Copy 设置页的介绍卡片新增“点击启用”标签，与 Image Download 和 Video Download 的启用提示保持一致。

## 3.1.6 — 2026-08-30

- Native Scroll 与 No Autoplay 的介绍卡片重新采用以动词开头的核心功能说明，并将功能介绍与默认状态、网站例外说明分成两个自然段。

## 3.1.5 — 2026-08-30

- 在英文与简体中文 README 顶部加入 Cosmic Gemini 标志，并将项目标题居中显示。

## 3.1.3 — 2026-08-30

- 重新设计 Native Scroll 与 No Autoplay 设置页。页面会依次引导用户设置全局默认状态、网站启用例外和模式例外。方向相反的规则现在会并列收纳在同一张卡片中，不再逐项向下堆叠。
- 将 No Autoplay 的声音自动播放权限独立为最后一步，并把右侧帮助内容改为与页面顺序一致的简明设置指南。
- Any Copy 强力模式在控制窗口中改为一级功能配色，未启用时会与 Any Copy、Image Download 和 Video Download 保持相同的视觉层级。

## 3.1.2 — 2026-08-30

- 重新整理英文与简体中文 README，将功能说明、使用方法、网站规则、隐私信息和兼容性内容拆分为主题明确的短段落，方便浏览和查找。

## 3.1.1 — 2026-08-30

- 将控制窗口重新设计为两行，每行四个按钮，并把全部设置移动到 Cosmic Gemini 名称右侧。第一行依次排列 Native Scroll、Native Scroll 强力模式、No Autoplay 和 No Autoplay 强力模式，第二行依次排列 Any Copy、Any Copy 强力模式、Image Download 和 Video Download。
- 控制窗口中的功能状态统一使用中性色和蓝色。一级功能未启用时比强力模式等二级功能更加醒目。持续影响页面的功能开启后会显示蓝色背景，Image Download 与 Video Download 的当前标签页会话则只显示蓝色。
- Native Scroll 与 No Autoplay 新增当前网站的单独设置。即使全局设置已关闭，也可以为某个网站单独启用功能。即使全局设置已开启，也可以单独停用。关闭当前网站的强力模式后，该网站会恢复使用标准模式。
- Native Scroll 与 No Autoplay 设置页新增单独启用的网站、单独停用的网站、强力模式网站和标准模式网站。精确规则可以覆盖范围更大的通配规则，同一范围内发生冲突时会采用停用或标准模式。
- Any Copy 与 Any Copy 强力模式的网站规则图标改为与标题文字使用相同颜色。
- Image Download 与 Video Download 的工作区名称前新增 Cosmic Gemini 标志和对应的功能图标。
- 全部设置页面在语言选项下方新增“重置所有设置”。确认后会恢复各项功能设置和网站规则，将界面语言恢复为 Chrome 的默认语言，并结束当前临时任务。已下载文件不受影响。
- 停止 Video Download 会话时会一并清理临时媒体文件。

## 2.1.3 — 2026-08-30

- 即使 Video Download 会话仍在运行，重新打开控制窗口时也会停留在主菜单。只有再次点击 Video Download 后才会进入媒体列表。
- 放大 Image Download 图标中的图片方框，并将其稍微移向左上方，同时保留原有的下载标志。

## 2.1.2 — 2026-08-30

- 放大并重新定位 Any Copy 强力模式图标中的实心闪电，同时增加与复制方框之间的分隔轮廓，使各种状态下的图标都更容易辨认。

## 2.1.1 — 2026-08-30

- 将控制窗口重新设计为紧凑的三行布局。第三行依次排列 Any Copy、Any Copy 强力模式、Image Download、Video Download 与全部设置。
- 在原有 Any Copy 图标右下角加入实心闪电，形成 Any Copy 强力模式图标。Any Copy 与 Any Copy 强力模式分别使用自己的中性色和绿色状态。
- 将 Any Copy 与 Any Copy 强力模式拆分为各自独立的设置对象、网站规则、主页面运行程序、隔离页面桥接程序、事件命名空间和活动状态。两项功能可以单独运行，也可以同时开启。
- 重新设计 Any Copy 设置页面，分别提供 Any Copy 网站与 Any Copy 强力模式网站两个区域，并在标题旁显示对应图标。
- 在设置页面最右侧新增全部设置入口，并提供集中导航页，可直接前往六个一级功能页面。
- Image Download 的整个筛选区域默认折叠，搜索、格式、布局、尺寸、排序和清除操作均收纳其中。标题会显示当前启用的筛选条件数量，刷新结果时仍会保留展开状态和已有条件。

## 1.5.19 — 2026-08-30

- 移除网页中的声音自动播放询问框。No Autoplay 现在会默认直接拦截声音自动播放。
- 新增默认关闭的全局声音播放开关，同时保留按网站设置的声音规则。两种权限都不会放行自动播放的视频。
- 支持识别自定义播放控件上的可信点击和键盘操作，手动播放视频时首次操作即可生效。

## 1.5.18 — 2026-08-30

- 英文界面和现行文档统一使用“audio autoplay”。
- No Autoplay 的声音播放询问固定为“继续拦截”和“本次允许”两个选项。

## 1.5.17 — 2026-08-30

- 全面检查控制窗口、全部设置页面、Image Download、Video Download 和 No Autoplay 声音询问中的用户操作控件。
- 强力模式按钮在命中精确规则或通配规则时，会直接移除相应规则。现在只有设置按钮会打开设置页面。
- 避免快速重复点击造成开关来回切换、设置重复提交、图片或视频任务重复启动，以及声音选项重复发送。
- 设置保存失败后会恢复控件状态。声音选择无法保存时会保留询问窗口。Image Download 扫描进行期间不会重复启动扫描。

## 1.5.16 — 2026-08-30

- Native Scroll 或 No Autoplay 的白名单生效时，功能图标、总开关和强力模式按钮会使用一致的灰度。
- 点击已启用的白名单按钮时，会直接移除当前网站命中的精确规则或通配规则，不再意外打开设置页面。
- 多条规则同时匹配当前网站时，会优先使用精确规则，其次使用范围最具体的通配规则。

## 1.5.15 — 2026-08-30

- 控制窗口载入时会先为当前标签页配置 Image Download 侧边栏，避免配置与打开操作同时进行所产生的竞态。
- 侧边栏偶发打开失败时，不再自动创建或切换到独立标签页。只有在设置中明确选择，或从侧边栏中手动打开时，才会使用独立页面。
- 控制窗口保持打开时，不会重复应用未变化的侧边栏配置。

## 1.5.13 — 2026-08-30

- Video Download 的“正在处理”按钮左侧新增红色取消图标。
- 取消后会停止当前的网络读取和本地媒体处理，删除未完成的临时文件，并让所选格式恢复为可下载状态。Chrome 不会开始下载该文件。
- 已取消任务的延迟进度不会再将媒体卡片改回“正在处理”。

## 1.5.12 — 2026-08-30

- Image Download 默认在 Chrome 侧边栏中打开，查看和选择图片时可以保留原网页。
- 新增图片工作区打开位置设置，可选择侧边栏或独立标签页。侧边栏不可用时会自动改用独立标签页。
- 侧边栏中新增“在独立标签页中打开”操作，并重新适配窄侧栏下的筛选、选择和图片卡片布局。
- 图片工作区会与来源标签页保持绑定。当前图片会话结束后，该标签页的专属侧边栏也会停用。

## 1.5.11 — 2026-08-30

- 修复部分哔哩哔哩 CDN 返回完整响应而非 `206 Partial Content` 时产生的范围请求警告。
- 选中的独立视频轨和音轨会先流式写入本地临时文件，再进行合并。媒体处理不再依赖远程服务器的随机读取能力，也不会将完整来源保存在内存中。
- 开始最终合并前会检查本地轨道。主要地址无法读取时，会继续尝试对应的备用 CDN 地址。

## 1.5.10 — 2026-08-30

- Chrome 下载列表会沿用 Video Download 媒体卡片中显示的标题，不再显示本地临时文件的随机标识。
- 视频和音频下载均采用稳定名称，字幕文件还会在名称中保留语言信息。

## 1.5.9 — 2026-08-30

- 下载状态在后台刷新时，不再重新创建正在使用的清晰度选择器，展开的菜单会保持稳定。
- 重新设计 Video Download 的结果页面。窗口适当加宽，并将视频预览、精简后的清晰度选择、格式信息和下载操作整合为一张媒体卡片。
- 同一档画质只保留一个优先选项，不再在菜单中重复显示编码和内部轨道。选中项目的格式信息仍会显示在菜单下方。
- 哔哩哔哩或 YouTube 提供兼容音轨时，可以单独选择“仅音频”下载。

## 1.5.8 — 2026-08-30

- 修复哔哩哔哩视频已经识别到格式，却在读取分离的音视频轨道时下载失败的问题。插件会通过临时媒体目录规则传递来源页面信息。
- 完成本地处理后会移除相应的临时规则。主要 CDN 无法读取时，仍会继续尝试哔哩哔哩提供的备用地址。
- 新增当前视频的缩略图和标题，便于下载前确认内容。新增清晰度选择器，可直接选择检测到的画质和兼容编码。
- 修复页面监听器读取 JSON 模式 `XMLHttpRequest` 时访问不可用的 `responseText` 所产生的错误。

## 1.5.7 — 2026-08-30

- 明确 No Autoplay 白名单会允许视频、音频和 Web Audio 自动播放，并且不会显示声音播放询问。
- 当前网站加入白名单后，如果声音播放询问已经显示，会立即将其关闭。

## 1.5.6 — 2026-08-30

- 修复哔哩哔哩页面将播放信息写入文档后又移除全局对象，导致 Video Download 无法识别视频的问题。
- 新增公开视频信息查询作为备用方案。即使播放器尚未开始播放，或 No Autoplay 正在运行，也能识别可用格式。
- 读取分离的音视频轨道时保留并尝试哔哩哔哩提供的备用 CDN 地址，提高本地合并的成功率。

## 1.5.5 — 2026-08-30

- 将媒体列表共用的刷新图标改为单个环形箭头。

## 1.5.3 — 2026-08-30

- 缩短 Video Download 媒体列表的标题栏，并将名称改为左对齐。
- 参照设置页面的名称比例，调整 Cosmic Gemini 一行的字号。

## 1.5.2 — 2026-08-30

- 移除 Image Download 图片页面标题区域中的来源网页名称。
- Image Download 与 Video Download 的结果页面名称中加入 Cosmic Gemini。
- 控制窗口中的英文标题采用与设置页面一致的字距。
- 当前网站匹配精确或通配白名单规则后，白名单图标会立即显示为绿色，Native Scroll 或 No Autoplay 不会在该网站启动，也不再额外判断页面效果。

## 1.5.1 — 2026-08-30

- 新增按需启用的 Image Download，仅在当前标签页中查找图片，并打开专用页面供您预览、筛选、选择和下载。
- 新增对响应式图片、延迟加载来源、链接中的原图、CSS 图片、开放式 Shadow DOM、框架、内嵌 SVG、Canvas 内容、结构化数据，以及当前会话中图片响应的识别。
- 将相关尺寸与格式归入同一组，默认推荐更接近原图的来源，同时保留其他可选版本。
- 新增普通扫描、深度扫描、可见区域截取、本地 JPEG、PNG 与 WebP 格式转换、分别下载和本地 ZIP 批量下载。
- Image Download 的候选来源和临时文件仅在当前会话中保留。站内跳转会继续运行，手动停止、关闭来源标签页或离开网站后会自动清理。
- 新增控制窗口第四行、Image Download 设置页、完整的中英文界面，以及相应的技术和验证说明。
- 重新绘制媒体列表共用的刷新图标，采用对称的双箭头环形结构。
- 同一网站保持打开期间，No Autoplay 只会询问一次声音播放方式，刷新或站内跳转也不会重复询问。
- Native Scroll 与 No Autoplay 在白名单网站上会降低功能图标和总开关的视觉强调。正在生效的白名单按钮使用蓝色选中背景和绿色图标。
- 移除 README 中 Image Download 与 Video Download 的背景段落。

## 1.3.10 — 2026-08-30

- Video Download 媒体列表刷新状态时，会保留“其他格式”的展开状态。

## 1.3.9 — 2026-08-30

- 新增分离音视频轨道的本地合并，并根据兼容的编码组合生成 MP4 或 MKV 文件。
- 新增 YouTube 与哔哩哔哩专用识别，支持哔哩哔哩 WBI 播放请求、哔哩哔哩国际站视频、YouTube 签名处理、字幕和本地轨道配对。
- 完善 HLS 与 DASH 处理，支持独立音轨、内嵌清单、直播片段、SegmentTemplate、SegmentTimeline，以及采用兼容封装方式的 HLS 清单。
- 新增针对 Vimeo、Facebook、Instagram、OK、VK Video、Canva、iQIYI、TwitCasting、Osmosis、Kick 和 Chaturbate 播放来源的本地适配。
- 视频文件会通过本地离屏处理器传输，使需要登录状态或来源页面信息的媒体能够沿用当前页面的请求环境。
- 将 Mediabunny 与 YouTube.js 随插件保存在本地，媒体处理不依赖运行时 CDN 或私有规则服务。

## 1.3.8 — 2026-08-30

- 移除 Video Download 媒体列表顶部的网页标题。

## 1.3.7 — 2026-08-30

- 播放器元数据或 HLS 清单已经提供媒体时长时，Video Download 会在列表中显示时长，不会为了计算时长而下载视频内容。

## 1.3.6 — 2026-08-30

- 重新设计 Video Download 的刷新图标，采用更加对称、平衡的圆形箭头。
- 服务器响应或 Resource Timing 已经提供媒体大小时，会直接显示文件大小，不会为了计算大小而预先下载媒体内容。

## 1.3.5 — 2026-08-30

- 将 Video Download 的下载标志移至原月亮所在的位置，同时放大箭头并缩短箭杆。

## 1.3.3 — 2026-08-30

- 参考 No Autoplay 的播放图标重新设计 Video Download 图标，并将月亮替换为实心下载标志。

## 1.3.2 — 2026-08-30

- 调整 Video Download 功能图标，将播放与下载标志分开，小尺寸显示更加清晰。

## 1.3.1 — 2026-08-30

- 新增按需启用的 Video Download，仅在当前标签页中查找视频，并在点击后直接打开媒体结果页面。
- 新增浏览器级与隔离页面双重识别，可发现视频文件、嵌入式页面、HLS 和 DASH 清单。
- 新增视频文件下载，以及完全在本地完成的 HLS 流式合并，支持主清单、多种画质、字节范围、分段 MP4、MPEG-TS 和标准 AES-128。
- 新增 Video Download 设置和临时会话状态，站内跳转时自动继续，离开网站后自动清理。

## 1.2.17 — 2026-08-30

- 移除设置页面异步等待整页显示的方式，避免刷新时出现全屏闪烁。
- 在首个可见画面前同步绘制已完成本地化的页面结构、导航图标、版本、默认控件和缓存的网站规则。

## 1.2.16 — 2026-08-30

- 设置页面曾改为等待语言、导航、网站规则和保存状态准备完成后再显示，以减少内容跳动。
- Native Scroll 与 No Autoplay 的开关默认直接显示为开启，同时仍会在页面显示前恢复用户已保存的关闭状态。

## 1.2.15 — 2026-08-30

- 将 README 中每项专属 Satellite 的背景统一为三句话，依次介绍外部机制、用户面临的问题和对应的解决方式。

## 1.2.13 — 2026-08-30

- 完善 Bili Daily Login 的 README 与设置说明，先介绍哔哩哔哩每日硬币机制，再说明插件的日程安排。

## 1.2.12 — 2026-08-30

- 取消 README 中“背景”标签的加粗格式，让功能介绍更加简洁。

## 1.2.11 — 2026-08-30

- 在 README 的功能介绍中，将各项 Satellite 分别列为子类别。
- 为每项具体产品补充简短的背景段落，说明其所解决的问题。

## 1.2.10 — 2026-08-30

- 将 Any Copy 的“执行网站”改为“标准模式网站”，与“强力模式网站”形成对应。

## 1.2.9 — 2026-08-30

- 将语言标题与选择器放在右侧栏的同一行，减少设置页面占用的空间。

## 1.2.8 — 2026-08-30

- 移除设置页中多余的语言说明，仅保留全局语言选择器。

## 1.2.7 — 2026-08-30

- 重新整理各项功能设置页的首个方框，以产品名称和标准模式作为标题，并为 Any Copy 单独标明按网站启用。
- 将英文设置界面的模式名称统一为“Enhanced mode”，并适当增加英文标题字距。
- 将语言选择器移至设置页右侧栏底部，并为 Satellites 新增独立的页面介绍方框。

## 1.2.6 — 2026-08-30

- 简体中文界面统一使用“强力模式”表示范围更广的处理模式，英文界面保持不变。

## 1.2.5 — 2026-08-30

- 调整 Native Scroll“工作方式”的首句，以 Native Scroll 作为明确的主语。

## 1.2.3 — 2026-08-30

- 优化简体中文界面的标点和语句，使表达更加自然。
- 调整 Bili Daily Login 的首句，以哔哩哔哩作为主语。

## 1.2.2 — 2026-08-30

- 调整 Satellites 自绘图标，使主星呈现平衡的水平与垂直轴对称造型。
- 完善 Satellites 右侧介绍栏目和 Bili Daily Login 的功能说明。

## 1.2.1 — 2026-08-30

- 新增 Satellites，并将其固定为最右侧的设置入口，用于收纳无需在控制窗口中操作的可选功能。
- 新增默认关闭的 Bili Daily Login，按北京时间每天 00:05 在后台安排登录，仅补做当天任务，并且不会监测哔哩哔哩页面。
- 将每项 Satellite 的开关、功能说明和独立隐私说明集中在各自的设置方框中。
- 将设置页面中的通用隐私说明改为各项功能自己的隐私说明。

## 1.1.2 — 2026-08-30

- 移除设置页面延迟显示的空白画面，并避免首次载入时重复渲染页面。
- 在设置页面首次显示前应用界面语言，同时减少功能页面切换时的跳动。

## 1.1.1 — 2026-08-29

- 避免网页注册已被禁止的 `unload` 监听器时被 Chrome 记录为 Native Scroll 扩展错误。
- 优化标签页关闭时的异步处理，避免刷新页面设置或工具栏状态产生错误。

## 1.1.0 — 2026-08-29

- 新增 Any Copy，可按网站恢复文字选择，并确保复制内容不被网页篡改。
- 新增 Any Copy 增强阅读模式，并支持从关闭状态直接进入和退出增强模式。
- 将当前界面与内部实现中的“强力模式”和“强力模式网站”统一更名为“增强模式”和“增强模式网站”。
- 为 Native Scroll 与 No Autoplay 新增三级状态颜色，并为 Any Copy 新增绿色的网站启用状态。
- 将扩展简介更新为“A personal toolkit for the web.”。

## 1.0.0 — 2026-08-29

- 将 Native Scroll 与 No Autoplay 整合为 Cosmic Gemini 中的两项独立功能。
- 为两项功能分别提供开关、白名单和按网站启用的强力保护。
- 新增视频和声音自动播放拦截，以及临时和永久声音权限。
- 新增可相互切换的独立设置页面、自然的中英文界面和临时权限自动清理。
- 延续 Native Scroll 的视觉设计和工具栏拦截状态提示。

## 0.1.0 — 2026-08-29

- 新增全局标准模式与强力模式。
- 新增精确主机名和通配符白名单管理。
- 新增极简控制窗口、完整本地化的设置页面和系统外观适配。
- 新增当前页面滚动代码被拦截后的工具栏状态提示。
- 新增精简验证和项目文档。
