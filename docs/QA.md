# Verification

## Automated checks

From the project directory, run:

```sh
npm test
npm run check
```

The focused suite covers configuration isolation, current-site overrides, exact and wildcard rule precedence, independent Any Copy runtimes, No Autoplay audio permission, direct media-play intent, Mailto Capture parsing and lifecycle cleanup, Page Display composition and cleanup, XHS text-image classification and photographic preservation, image grouping and original-source ranking, download-discovery lifecycle states, Bilibili calendar-day handling, locale selection, page runtimes, Manifest V3 permissions, local-only assets, script syntax, and interface constraints.

## Chrome checks

1. Open `chrome://extensions`, enable Developer mode, and load the `extension` folder as an unpacked extension.
2. Open the popup and confirm that it contains five paired rows: Native Scroll with Native Scroll Enhanced, No Autoplay with No Autoplay Enhanced, Any Copy with Any Copy Enhanced, Image Download with Video Download, and Reduce White Point with Greyscale. Confirm that the product icons are slightly larger than the All Settings icon, the Cosmic Gemini wordmark is hidden, and All Settings occupies a separate bottom row aligned to the right.
3. Confirm that inactive primary products use a stronger neutral color than Enhanced controls. Active Native Scroll, No Autoplay, Any Copy, Any Copy Enhanced, Reduce White Point, and Greyscale controls must be blue with a background. The two Page Display controls must change their global settings even when the active tab is a Chrome page where page injection is unavailable. Active Image Download and Video Download controls must be blue without a background. Confirm that dark mode uses the brighter blue throughout the popup, settings pages, and download workspaces without changing the light-mode color. Solid blue buttons must retain readable foreground text. After Native Scroll or No Autoplay actually intervenes, confirm that the active Standard or Enhanced control gains a thin blue outline.
4. On a fresh installation, verify Native Scroll, No Autoplay, Mailto Capture, and Website Knowledge Control and its categories all begin off, while saved explicit choices remain intact after upgrading. Then activate Native Scroll from the popup on one website. Confirm that an exact Always Standard rule keeps that website active. Turn the global default on, make the same website inactive from the popup, and confirm that its exact Always inactive rule takes priority. Repeat with No Autoplay.
5. Add `*.example.com` under Always use Enhanced mode, then turn Enhanced off on a matching exact hostname. Confirm that the website receives an exact Always use Standard mode rule. Turn Enhanced on while the global product default is off and confirm that the exact website becomes active in Enhanced mode.
6. Open all seven product settings pages and the All Settings page. Refresh them quickly and switch among them. Confirm that the structure, cached control values, and en-US or zh-CN copy appear together without a blank or wrong-language frame. Confirm that the first introduction card on every page uses the same blue-tinted border. Confirm that Page Display uses the brightness-sun navigation icon immediately before Satellites, All Settings remains the rightmost switch, and all seven product cards open the correct pages.
7. From All Settings, choose Reset all settings and cancel once. Confirm that nothing changes. Confirm the next attempt and verify that product defaults, website rules, explicit language choice, active image and video sessions, and temporary toolbar state are reset while already-downloaded files remain available.
8. On a page with a wheel takeover handler, confirm that Native Scroll preserves browser scrolling, that the browser toolbar keeps the standard Cosmic Gemini mark, and that its title and the popup state reflect current activity.
9. In each Native Scroll and No Autoplay Website settings card, add one rule to Always inactive, change it directly to Always use Standard mode, then to Always use Enhanced mode, and finally remove it. Confirm that the rule appears in only one category at a time. Add a broad wildcard and a more-specific exact rule with different behaviors, then confirm that the exact rule takes priority. For No Autoplay, confirm that Always inactive permits video, audio, and Web Audio autoplay.
10. Add a website to the shared whitelist from Native Scroll settings and confirm that the same rule appears in No Autoplay settings. On a matching page, confirm that both popup controls are unavailable and neither runtime leaves page API wrappers, behavioral event hooks, observers, styles, media pauses, or removed media behind. Remove the rule from No Autoplay settings and confirm that it disappears from both pages and the previously saved product-specific behavior resumes. Confirm that click-activated products remain available when explicitly started.

    Start from All Settings or Satellites, use the in-page navigation to reach Native Scroll, and add `*.douyin.com`. Confirm that the rule saves, applies to the root domain and its subdomains, and can be removed after switching to No Autoplay without reloading. Repeat from another settings entry. Invalid input must show a format error before submission; a failed save must show a save error, preserve the input, and allow a manual retry.
    Repeat rule edits with `*.163.com` and an internationalized domain. Confirm that unchanged lists keep their dropdown controls during refreshes. With delayed or failed state reads, a confirmed save must remain visible, failed edits must restore saved values, and input errors must remain visible after a previous save attempt. While a dependent slider is saving, turn its parent feature off and confirm that the slider remains disabled afterward. A failed language change or reset must show an error without silently repeating the command. If writing defaults fails, existing preferences must remain stored. If a media-view connection drops just before leaving the list, its delayed reconnect must not make the hidden list visible to the scanning lifecycle. Stop Image Download during a delayed scan and confirm that the late result cannot reactivate its controls. Opening the popup in Incognito must not change the ordinary Settings preview.
11. On a page that automatically calls `video.play()`, confirm that No Autoplay stops the video. Click both native and custom playback controls and confirm that the first trusted click starts playback and updates the page control normally.
12. On a page that starts audible audio or Web Audio automatically, confirm that No Autoplay blocks it without showing a page prompt. Turn on **Allow audio autoplay on all sites** and confirm that audio elements and Web Audio may start everywhere while autoplaying video remains blocked. Turn it off, add a website under **Always allow audio autoplay**, and confirm that only matching websites receive the same audio permission.
13. Add a hostname to No Autoplay Enhanced sites and confirm that existing and later video or audio elements are removed.
14. On a page that disables selection or changes copied text, click the Any Copy product icon. Confirm that only its icon becomes blue with a background and the original selection reaches the clipboard without an added suffix.
15. Click Any Copy Enhanced while Any Copy is off and confirm that only the enhanced icon becomes blue with a background. Open another tab on the same website and confirm that Enhanced remains off there. Enable Any Copy as well, then turn either product off and confirm that the other remains active.
16. Confirm that Any Copy Enhanced produces a selectable static reading view, preserves useful text and images, remains active through navigation in the same tab, and restores the original page without reloading when turned off. Close the tab and confirm that its session is removed.
17. On a page containing regular, responsive, lazy-loaded, linked, CSS, SVG, and canvas images, open the popup and click Image Download. Confirm that the already-prepared workspace opens in Chrome’s Side Panel without replacing or leaving the source tab, remains bound to that tab, and uses a usable single-column layout at a narrow width. Repeat several times and confirm that no separate workspace tab appears after a transient Side Panel error. Confirm that related variants stay grouped, the largest valid original candidate is selected by default without hiding alternatives, and no recommendation badge or label is shown.
18. Confirm that Image Download's complete Filters card, including search, opens collapsed by default. Expand it and filter results by text, format, layout, dimensions, and sorting. Confirm that the active-filter count includes search, that a candidate refresh preserves the expanded state and all values, and that clearing filters returns the count to zero. Select visible results and verify separate and ZIP downloads. Confirm that original format is preserved by default and that JPEG, PNG, and WebP conversion happens only after selection.
19. Use Image Download’s area capture, normal rescan, and deep scan. Confirm that the selected visible region becomes a result, the rescan icon is a single circular arrow, deep scan reveals compatible lazy images, and the source page returns to its original scroll position. During a scan, confirm that both scan controls remain unavailable and rapid clicks do not start another scan. Confirm that rapid clicks on open, capture, stop, or download controls also produce one action. While the Side Panel is scanning, click its red stop control and confirm that scanning feedback disappears immediately before the panel exits with a smooth close transition. Use the Side Panel action to open the full workspace in a separate tab, then change the workspace-location setting and confirm the next session opens directly in that tab. Change the setting back to Side Panel and confirm that an opening failure leaves the source tab selected instead of silently creating a separate tab.
20. On a page with a direct MP4 or WebM source, click Video Download. Confirm that its icon becomes blue without a background and its result view opens immediately. Confirm that the result header shows the Cosmic Gemini mark, the Video Download icon, and the compact product wordmark in that order. Verify that the selected file is streamed locally before being handed to Chrome Downloads. Return to the main popup while the session remains active, close the popup, and open it again. Confirm that it stays on the main view until Video Download is selected again.
21. On a page with a compatible HLS master playlist, confirm that available variants and alternate audio are shown, the selected playlist is assembled without loading the complete output into memory, and its temporary artifact is removed when Chrome finishes or interrupts the download.
22. With No Autoplay enabled, open `https://www.bilibili.com/video/BV1yM4y1t7zB/` without starting playback. Confirm that Video Download shows the video thumbnail and title, presents one concise option for each main quality, and keeps an open quality menu stable while event-driven status updates arrive. Start a selected format and use the red cancel icon while it is being prepared. Confirm that processing stops, no Chrome download begins, partial local files and media-header rules are removed, and the format can be selected again. Then complete a selected video after local audio-video merging and confirm that a CDN returning a complete response without Range support produces no Mediabunny warning. Confirm that Chrome uses the displayed video title rather than the temporary artifact identifier as its filename. Choose Audio only and confirm that the standalone track uses the same title when available. Repeat on YouTube and confirm that combined or locally remuxed formats and subtitles remain available.
23. On a compatible unprotected DASH page, confirm that representations are expanded and selected audio and video tracks produce one MP4 or MKV file. Confirm that a protected representation remains unavailable.
24. Confirm that Image Download and Video Download continue across same-origin navigation and clear old candidates. Close or hide each download workspace, wait two minutes, and confirm that discovery pauses while existing results remain available. Reopen it and confirm that discovery resumes with an immediate page scan and no timer-based UI polling. Confirm that cross-origin navigation, source-tab closure, or the stop control ends the session.
25. Add a wildcard website behavior that covers the current website. Use the popup controls and confirm that they create an exact-host behavior rather than deleting the broader wildcard. Click the primary control again to remove the exact behavior and confirm that the broader rule applies again. Repeat with a wildcard Any Copy rule, then confirm that Any Copy Enhanced neither reads nor writes that rule and remains limited to the current tab.
26. Rapidly click each popup state control twice and confirm that only one action is accepted. In Settings, repeat with switches, selects, Add, and rule removal controls. Confirm that one save occurs and that a failed save restores the last confirmed value.
27. Navigate away and confirm that the toolbar title returns to its default state while the standard Cosmic Gemini mark remains unchanged.
28. Open Satellites and enable Bili Daily Login while a Bilibili account is already signed in to Chrome. Confirm that daytime and evening checks are scheduled without opening or inspecting a Bilibili tab, that each check uses the existing account session and verifies the daily reward result, that a restart does not repeat a completed window, and that disabling the feature clears its alarm and temporary request-header rule.
29. Inspect the loaded extension and confirm that `content/central-page.js` is the only declarative content script and `background/central.js` is the only service worker. Confirm that central contains routing decisions while province modules coordinate their assigned products and product modules own execution. With individual products inactive, confirm that their main-world runtime symbols and isolated bridges are absent. Activate and then deactivate Native Scroll, No Autoplay, Any Copy, and Any Copy Enhanced one at a time; confirm that only the selected product is injected and that its runtime, listeners, observers, styles, and page changes are removed afterward. Confirm that Image Download and Video Download inject scanners only after their explicit controls are selected, and that disabled Satellites tasks create no work.
30. Before activating either download product, inspect a resource-heavy tab and confirm that no Image Download or Video Download `webRequest` listener is registered for it. Open Image Download and Video Download in separate tabs, then close both workspaces. Confirm that each exact tab keeps its browser-filtered response listener and continues discovering resources during its two-minute grace period. After one session pauses, confirm that its listener is removed without affecting the other tab. After the last session pauses, confirm that all download response observation is released while both candidate lists remain stored. Reopen either workspace and confirm that only its source tab is registered before a new resource loads and that the page is rescanned immediately.
31. Allow the extension in incognito, then open its popup and settings from an incognito window. Confirm that no ordinary settings or website rules appear, Native Scroll and No Autoplay begin off with “Disabled by default in Incognito.”, and their Recommended setup and Privacy content remain visible. Confirm that Bili Daily Login shows “Disabled in Incognito” instead of a switch. Explicitly activate a page product, verify that it affects only the incognito session, then close every incognito window and start a new session. Confirm that all products are inactive again and that ordinary-window settings never changed.
32. With Native Scroll enabled, open the Xiaohongshu Explore feed and select several image and video post cards. Confirm that each card opens, its media navigation remains responsive, and closing the card returns to the feed. Confirm that Native Scroll still uses its configured behavior on a non-Xiaohongshu test page.
33. On a page that registers a scrolling takeover while Native Scroll is active, disable Native Scroll and confirm that the website regains its original behavior and that no Native Scroll hooks remain. Re-enable Standard mode without reloading the page and confirm that the earlier takeover is suppressed again. Repeat with Enhanced mode, then reload the page and confirm that no retained metadata leaks into the new document.
34. Open Satellites and confirm that Ad Marshal shows compact ordinary checkboxes only for Tencent News and Zhihu. The interface must not list implementation subdomains separately. Every checkbox must begin unchecked in ordinary and incognito windows. Leave one group unchecked while selecting the other, then load both sites and confirm that only the selected group creates an Ad Marshal bridge, main-world runtime, or session DNR rule. Unknown or deprecated stored values must not select a site.
35. Enable Ad Marshal and test `https://news.qq.com/`, a `https://view.inews.qq.com/timeline/…` page, and `https://www.qq.com/`. Confirm that the first two hosts receive the same article policy and that `www.qq.com` receives its separate home-page policy. Each page must render normally, keep its application root populated, and report no DOM reconciliation error such as `NotFoundError`. On the timeline host, confirm that `/qqcdn/news-share/js/custom_*.js` receives the local `emonitor` compatibility script, its inline bootstrap produces no missing-API error, and `n.ssp.qq.com` requests resolve locally. On both article-policy hosts, confirm the shared reporting loader receives a local resource; on `news.qq.com`, also confirm the article-site `emonitor_` and advertising loaders receive local resources. On `https://news.qq.com/rain/a/20260905A09FA600?adChannelId=news`, confirm that Ad Marshal pauses and releases media before deleting `.qqcom-jxvideo`, its outer `.video-wrap`, and the Tencent Video bridge frames while preserving the `#content-right.content-right` root. On `https://news.qq.com/rain/a/20260905A09XWD00`, disable No Autoplay and confirm that Ad Marshal still pauses automatic audio and video, removes `autoplay`, applies `preload="none"`, and keeps `.txp_poster_img` visible before user activation. Click the article player and confirm that media loading resumes, playback begins muted, and an explicit unmute remains effective. Scroll until the player enters `.videoPlayerMini` and confirm that the floating state becomes hidden and paused without removing its node, video or source URLs, or poster. Return to the inline position and confirm the same player can be used again. The added-node and media-attribute observer must not rescan unchanged content, the floating-player observer must remain scoped to `.qnt-p`, and both observers plus every media guard must stop when the policy is disabled. These additions must not apply to the timeline host. On `www.qq.com`, confirm the `/www/js/emonitor/` loader, `h5.ssp.qq.com` advertising assets, and confirmed game-advertising column are neutralized. Shared telemetry calls, advertising endpoints, tracking images, and the `127.0.0.1:11601/check` probe should resolve locally without repeated failures or a loopback connection attempt. Other advertising containers should be hidden without being physically removed. Disable the Tencent News setting and confirm that request rules and running code stop immediately; removed or hidden areas may remain changed in the current document and must return after refreshing. Confirm that Settings continues to list only the existing Tencent News entry, that another subdomain such as `video.qq.com` does not activate either policy, and that navigating away removes the tab-scoped rules.
36. With Ad Marshal enabled, test `https://www.zhihu.com/` plus another first-party subdomain such as `https://zhuanlan.zhihu.com/`. Confirm that the Sentry and ZA analytics loaders receive local empty scripts and that matching requests to the analytics, APM, experiment, crash-reporting, and Baidu Analytics hosts resolve locally without repeated `Failed to fetch` console errors. Confirm that feeds, answers, articles, images, sign-in, messages, and account controls remain available. Confirm that `zhimg.com` resources are unaffected and that Ad Marshal uses no DOM observer, timer, or content-node removal.
37. Open Satellites in an ordinary window and confirm that Mailto Capture appears first and begins enabled. Test a single-address link and confirm that the popover shows selectable plain text with Copy address, never opens a mail application, and closes on an outside click or Escape. Test multiple To recipients plus repeated CC and BCC fields, subject, body, an additional query field, encoded line breaks, and a literal plus sign. Confirm that all values appear, Copy address is absent, and Copy message preserves the structured content. Repeat with a dynamically inserted link and a link inside a frame. Disable Mailto Capture and confirm that its bridge, main-world runtime, listeners, and popover are removed. In a fresh incognito session, confirm that it begins disabled and can be enabled only for that temporary session.
38. Open the dedicated Page Display settings page and confirm that its brightness-sun navigation icon appears immediately before Satellites. Confirm that the master switch, Reduce White Point icon, and Greyscale icon render in the first frame. Confirm that each child switch is aligned directly to the right of its feature heading, with only the Reduction control shown as a subordinate row. While the master is off, every child switch and slider must be disabled and both child cards must share one clear dimmed state, while their saved selections remain visible. Enable the master, then enable each feature separately and together on an ordinary page. Confirm that text, images, Canvas, animations, embedded content, and video receive the selected effect without layout, scroll, selection, focus, pointer, or keyboard changes. The Reduction control must remain disabled while Reduce White Point is off and support the full 10–80 percent range when enabled. Turn the master off with both children selected and confirm that the page immediately returns to its prior appearance, all child controls become unavailable, and the visual layer, fullscreen listener, bridge, and runtime are removed. Turn the master back on and confirm that both saved child selections resume. With the master off, enable either child from the popup and confirm that the master and selected child turn on together. Test Reduce White Point on a page whose page-wide dark mode applies a root `invert(...)` filter, then switch that theme on and off while the product remains active. Confirm that dark areas never become lighter and the overlay follows the rendered filter direction without a reload. Turn off Reduce White Point while leaving Greyscale active and confirm that its root, body, and head appearance observer is disconnected. Turn off both children and confirm that the page returns to its prior appearance. Verify that the product has no content observer, polling timer, network request, page API wrapper, or DOM scan.
39. Open Satellites and confirm that XHS Image Dark Mode appears second and begins disabled. Confirm that every subordinate setting is disabled and visually dimmed while the main switch is off. Enable it, turn off Show image controls, and confirm that the Button opacity row disappears without losing its saved value. On `www.xiaohongshu.com`, confirm that its contextual open-book-and-bulb icon appears below the fixed popup rows and nowhere else. Enable page-wide dark mode and verify that actual dark viewport surfaces are recognized before extension-specific markers. Confirm that bright text cards, including cards with pink, blue, green, yellow, or dark frames and layouts combining broad light and dark panels, receive a whole-image dark treatment while ordinary portraits, scenery, framed photographs, mixed photo-and-text images, and blank cards remain unchanged. Continue through several feed updates and confirm that ordinary photographs leave image observation after classification, transformed cards remain reversible, background work yields between tasks, and dormant images do not occupy either worker. Confirm that feed covers never load or display image controls and that scrolling performs no control-position pass until an expanded-view control exists. Open a multi-image post whose feed cover remains light and confirm that the full first image and every later slide are still classified independently, with the active and adjacent slides handled first. Confirm that only expanded-view images enter resize observation, only the active image's control is visible immediately left of the page count, and that it switches that image independently between light and dark. Press and hold the control, confirm that all images in the post plus its feed cover return to their original display, the control disappears, and closing and reopening that post keeps it paused. On a user profile page, use the profile switch and confirm that every existing cover returns to its original display, pending analysis stops, newly loaded posts are not analyzed, and turning the switch back on resumes handling for that profile. Confirm that single-image posts use the matching top-right position. Close and open several posts and confirm that old controls, viewer observation, disconnected records, load listeners, and queued work are released. Confirm that avatars and the red follow button are dimmed without inversion. Change the control opacity, hide all image controls, and verify each setting. On a light page, confirm that the hollow blue waiting state performs no image scan. Enable Always on and confirm that processing starts immediately and the popup fills the bulb and adds a blue background. Disable the product and verify that observers, styles, controls, listeners, queued work, page activity state, and temporary post and profile pauses are removed, and that any analysis finishing afterward cannot change the page.

## Document formats and Customs safety verification

- `npm test`: includes new format preflight, generated legacy Word/PPT containers, cycle/encryption rejection, XLS cached values and merged cells, RTF Unicode/fields, ODF repeats and external resource handling, EML MIME parsing and inert attachments.
- Independent temporary Chrome, without the user's browser profile: previews for `.doc/.xls/.ppt/.rtf/.eml/.odt/.ods/.odp`, plus `.docx/.xlsx/.pptx/.pdf` regressions. Legacy examples include Apache POI's public Word 97, Excel and PowerPoint samples. First-party fixture sources are in `test/fixtures/additional-document-formats.mjs`.
- Verified light/dark rendering, zoom, worksheet/slide navigation, loading feedback without a numeric percentage, and supported-format categories in both UI languages. Inspected narrow/wide whitelist screenshots.
- Hostile EML/ODF samples include remote images, frames, stylesheet imports and active HTML. No remote document requests occur, forbidden elements are absent from the displayed tree, and PDF Viewer blocks document scripts and remote requests inside its own sandbox.
- Independent Chrome also verified original-image retrieval, JPG conversion, safe ZIP member names, HLS playlist/segment assembly, artifact cleanup and rejection of non-capture URLs in the screenshot path.
- Verified processor requests from Settings are refused while Central requests work; verified original EML download bytes exactly match the source. Unit tests cover wrong-document URLs, empty/out-of-order/replayed cache chunks and streamed size limits independent of Content-Length.
- Verified Document Preview whitelist insertion order, help, reset, reload persistence and subdomain/port matching; adding a source page skips new capture, and disabling capture preserves existing previews. Access Control and Document Preview lists have two columns at 1000 px and one at 480 px.
- Verify Access Control's optional one-time visit with a blocked main-frame navigation: the Chrome error tab's toolbar button retries that address directly, whereas other tabs retain the normal popup. Leaving the blocked domain, disabling the option, or closing the tab removes temporary state and restores the popup.
- Formatting regression fixtures exercise legacy Word character/paragraph inheritance, direct overrides, lists, hidden text, table borders/merges and page metrics; BIFF8 fonts/palettes/XFs and row/column metrics; and binary PowerPoint text boxes/geometry/colors/spacing. Include invalid FKP offsets and overlapping spreadsheet merges.
- In independent Chrome, compare requested CSS fonts with `CSS.getPlatformFontsForNode` for mixed-font DOC/DOCX samples. Verify separate Chinese/Latin slots and explicit fonts overriding inherited themes, including table contents. Check that unavailable originals use local typeface substitutes without remote font requests; do not infer exact font restoration from CSS family names.
- Verify preserved spreadsheet cell sizes/fills, slide paragraph alignment/line height, rich text, hidden merge roots, ODF style-family isolation and EML passive presentation styles. Hostile EML CSS must not create remote requests or fixed-position overlays. Test every non-PDF family in both themes, part navigation, zoom and a narrow viewport.
- Check that consecutive ASCII/full-width spaces and underline-only blank fields survive conversion and display. Paragraph-mark sizes must affect spacing metrics but not change text-run sizes; absent paragraph margins are zero and package-local default tab intervals are used.
- Check capture-dialog sizes such as `1 byte`, `2 bytes`, `44.5 KB` and `1.5 MB`: decimal units, at most one decimal and no forced trailing zero. Conversion/performance comparisons use identical bounded fixtures and warm runs; track bundle growth and numeric formatter construction counts rather than asserting general browser speed from small samples.
- Test artifacts/scripts live under ignored `dist/qa/document-preview/`; logs and temporary browser profiles remain outside the shipped extension. No claim of exhaustive malware detection or full legacy Office layout fidelity.

## PDF Viewer

- Click or keyboard-activate the filename to check Document properties in both languages/themes and narrow windows. Test ISO dates with/without seconds, positive/negative/absent time-zone offsets, original dimensions after display rotation, safe long metadata, Escape/outside-click closure and cached reopening. With a short viewport, scroll properties to the bottom, close and reopen: focus returns to the heading and both scroll offsets reset to zero. Missing metadata and closure during a read must not affect the PDF.
- Delay document bytes: the themed toolbar/local worker should initialize first, with document controls disabled. After bytes arrive, page one must paint first without a whole-document render; leave/expire during preparation to verify cleanup.

- Use an independent Chrome profile; never reuse the user's browser for routine QA. Run `scripts/test-pdf-viewer-browser.mjs` with `PDF_VIEWER_CHROME` and `PDF_VIEWER_PLAYWRIGHT` pointing to the test browser/runtime. Optional `PDF_VIEWER_REAL_SAMPLE` and `PDF_VIEWER_PASSWORD_SAMPLE` add real/encrypted PDFs (test password `reader-test`).
- Check centered toolbar groups at wide/narrow widths, equal current/total page widths, retained previous/next controls, 110px zoom dropdown and correctly positioned custom zoom on pointer/keyboard opening. Verify one appearance button in both themes/languages, unchanged tooltip and no Default button. Toggle twice for Auto/Light/Dark defaults: the second toggle clears the site override and restores the default, with no reparse or reading-position change.
- Preview through Document Preview as well as the isolated host. Confirm 100% on every new open/reload; two plus presses yield 120%, two minus presses return 100%. Test 25–500% bounds, fit modes, Ctrl/Cmd shortcuts and pinch zoom. Rapid zoom must retain the previous canvas until sharper rendering completes, without changing the current page.
- Compare 7/9/11-point text and fine rules at 100% and fractional zoom on Retina. Check all 1× through 6× sampling choices, default 4× and immutable sampling in already-open readers after settings changes, including later zoom/navigation. Confirm 6× visible-area sampling when selected and adaptive detail density at 200%/500%, smooth detail scaling and canvas reuse for small scrolls. Wait for a newly completed detail canvas, not the old scaled canvas. Assert a 4-Mi-pixel base bound (1 Mi at 1×) and sampling²-Mi-pixel detail bounds for 1× through 6× and verify that navigation does not accumulate full-resolution offscreen page canvases. Screenshot pixels must map white paper to approximately #0a0a0a and black ink to #f5f5f5 in dark mode; light mode retains white/black. Measure both zoom input time and final render completion. Include fully visible fit-page and raster-only scanned PDFs. Check the sampling menu in both languages, saved first-frame values and disabled parent state.
- Rotate left repeatedly, including while zoomed/scrolled: preserve the current page, rotate counterclockwise and return to the initial orientation after four presses. Download remains byte-identical. Selection, internal destinations and safe links work after rotation and zoom; ⌘F/Ctrl+F remain available to Chrome without reader interception; document JavaScript, attachments, unsafe links, forms and editors stay inactive.
- Test Auto/Light/Dark and site-session overrides with the browser's theme opposite the selected preference. Capture first-open/reload frames: neither host nor child should paint a white loading page in dark mode. Dark paper has visible fine edge borders, and the surrounding viewer remains independently dark. Theme switching preserves page, zoom and the loaded worker.
- Check the filename and navigation group against the window midpoint, independent of the right controls. The PDF toolbar omits the source website. Theme/default controls sit below print/download. Check every icon at the same visual size, the compact fixed-width zoom select and a narrow viewport. Toolbar text is excluded from browser translation.
- Scroll across many pages and open/close the thumbnail sidebar; only visible/adjacent pages rasterize. Page/thumbnail canvas counts stay bounded, thumbnails stop when hidden and high-zoom visible details regain sharpness. Hiding the tab, closing the frame or expiring the source session must stop work and destroy the viewer worker; downloads and settings authorization remain with Document Preview.
- Test native print-dialog invocation manually (do not submit an actual print job), page ranges, cancellation and budget errors. Print output uses light paper, excludes controls/reading borders and does not alter originals. Test wrong/right passwords and cancel; no password is stored. A document failure still leaves original download available.
- Network inspection must show only bundled assets and authorized cached PDF bytes, never PDF-referenced remote content. Verify the reader has an opaque origin and no `chrome.runtime`, including frames created from the actual extension manifest.

## Merged preview toolbar

- Check short and long English/Chinese filenames in light/dark appearance at desktop and narrow widths. Keep the product name above Cosmic Gemini, center the filename over the source website, truncate filenames to one line with an ellipsis and a full-name hover title and omit file size. No empty feedback row or horizontal overflow after loading.
- Exercise document zoom/theme/download and spreadsheet/slide navigation; all controls must remain reachable, including with long part names. Verify loading, choice, failure and expiry feedback inside the toolbar and full-viewport PDF display.

### Word reading fidelity and toolbar translation

- Convert the deterministic reading fixtures and representative `.doc/.docx` files in an independent Chrome profile. Confirm the same OOXML rendering for `.docm/.dotx/.dotm`; macros and embedded programs must remain inert.
- Measure a twenty-space underlined run with a DOM Range: text length and visible width must both remain nonzero and all spaces must occupy width. Check trailing spaces, empty paragraphs, narrow-window wrapping and local font fallbacks in light/dark appearance.
- Verify Chinese/letter/decimal nested lists, style-linked levels without explicit `ilvl`, interrupted/continued sequences, separate restarts and Symbol bullets. At a narrow viewport, measure continuation-line left edges for long items and three/four-digit labels; confirm markers never overlap text and copied labels have a separator. Cover the binary DOC list fixture as well.
- Check direct italic/bold overrides, raised/lowered characters, contextual spacing, borderless/styled tables, image width/ratio, checkbox symbols, ruby annotations and inline/display fractions. Pass hostile MathML containing links, foreign HTML and event attributes through the sanitizer; preserve only passive math and issue no remote requests.
- Move the source tab and switch active windows before choosing Preview. Verify placement immediately to its right in the source window; repeat with remembered choices, reused cache from another source tab and the fallback choice page.
- The toolbar must retain `translate="no"` and `notranslate`, with no such marker on the document frame. Where browser translation is available, translate the body and verify that filename, source site and controls retain their UI language.

### Settings tab replacement

Open All Settings and different feature Settings pages from the popup, directly, through Duplicate and in another window. Only the newest Settings tab should remain; navigation among Settings pages and reload must keep that tab. Exercise rapid creations and an old tab navigating away before cleanup. Other extension workspaces, web pages and Settings in the other normal/incognito context must remain open.

For Settings developer mode, press `1` outside website inputs, switch feature pages and interface language, then press `1` to hide the information. Check product and child affiliation paths, selectable metadata without surrounding controls activating, narrow wrapping, and unchanged heading/icon/switch coordinates when the mode opens. Typing `1` in a website field must remain ordinary input. Refresh while the mode is on: no metadata should appear even for an intermediate frame, and the optional module should not load until the shortcut is used again. The focused reusable browser check is documented in ignored `test-dist/settings-developer-mode/README.md`.

For the Document Preview choice prompt, delay the Preview response and compare dialog/button geometry before and during loading in both languages/themes and a narrow viewport. The indeterminate line must appear left of Preview with identical bounds, no percentage/status row and paused animation when idle; failure re-enables actions and shows its error.

For PDF rendering feedback, deliberately delay a visible page’s render completion: confirm no immediate spinner/flash, then only the toolbar-divider progress line after a short wait. The workspace bounds must remain unchanged; release the render and verify the line disappears. Offscreen unfinished pages must not trigger it. Check the PDF.js page loading pseudo-element has no content/background in both themes.

PDF sandbox regressions must assert a real Worker port, not just successful rendering, and fail on fake-worker fallback or CSP/Permissions Policy warnings. Verify whole-document Copy uses the write-only policy while clipboard read remains denied (intercept writes in QA, never overwrite the user’s clipboard). Check modal title/input focus, top scroll position, Escape and outside dismissal, and prohibit `autofocus` attributes in the opaque reader. Exercise cancellation/disposal during worker startup and render errors.

### External Links Capture

The isolated PDF browser suite also exercises the non-PDF content sandbox: all output formats share this sanitizer/controller. Cover primary/middle/modifier/keyboard activation, internal anchors, web/app confirmation, copy-only mailto/tel/sms, inert unsafe links, discarded remote images/frames/scripts, part replacement and disposal. App confirmation tests cancel navigation after observing the trusted second action; never launch an installed application during QA. `npm test` also checks the shared parser, rejected target classes and existing Mailto Capture geography/copy regressions.

### Website Fixer

In Satellites, confirm Website Fixer is the final General features card and begins off. Its Translate Override setting and website list must be unavailable while the master is off; the list must also be unavailable while the subfeature is off. Add `ilsos.gov`, enable both switches, then reload an Illinois Secretary of State news page with both a `google/notranslate` meta declaration and `<html translate="no">`. Confirm both opt-outs are gone and Chrome can offer translation under its normal language preferences. Check delayed inserted elements and `.notranslate` classes, while unrelated classes and metadata remain intact. Turn either switch off and reload; the original declarations should remain. Test the root hostname and a subdomain separately, and check that no script is registered when the allowlist is empty.

For Website Fixer’s card, confirm the main heading and each child are separated by thin rules, and the child sections plus Privacy dim when the main switch is off. For Stay on the page, use `scripts/test-website-fixer-browser.mjs` with a disposable Chrome profile. Reusable synthetic fixtures and Settings screenshots belong in ignored `test-dist/website-fixer`, documented by its README. Check master/child defaults, bare-subdomain/URL normalization, punctuated saved count without domain rows, the red Confirm label after one Clear website list click, cancellation by clicking elsewhere or Escape, clearing only on the second click, three-second save-message expiry, and independent Translate Override state. Test page-driven links (including new targets), forms, script location changes, meta refresh, blobs, popups and history; same-site subdomains should still navigate and external embedded media should load without top-navigation/popup privileges. Confirm external server redirects are denied at the network layer, allowing for Chrome's blocked-page UI. Clearing the list and refreshing must remove the guards. Do not attach to the user's browser or run unrelated suites for this feature.

For Access Control, Document Preview and Website Fixer, paste an HTTP(S) URL or hostname with a path into the website input. Confirm that the saved entry is its eTLD+1 and applies to that domain and all subdomains; test a multi-label public suffix, a selected private suffix and an unlisted top-level label. After submitting invalid input, empty the field and confirm the validation message disappears immediately. Confirm that a bare subdomain still saves as that exact subdomain, Access Control aliases and IP inputs retain their existing behavior, and non-web URLs or credential-bearing URLs are rejected. The shared subdomain scope note appears only below a list containing a domain; an IP-only list has no note. Individual entries have no scope suffix. Across all Settings domain-rule forms, compare the input-to-first-entry spacing with an empty and a populated list; empty validation messages must be completely hidden even under card-specific paragraph styling, while actual errors remain visible. Empty-list rows should not reserve populated-row height.

## LeetCode Explore appearance

Run `node --test test/leetcode-dark-mode.test.mjs test/settings-page.test.mjs test/config.test.mjs` and the focused `scripts/test-leetcode-browser.mjs` with the existing `PDF_VIEWER_PLAYWRIGHT`/`PDF_VIEWER_CHROME` environment paths. The isolated fixture covers default-off settings, same-origin Explore/playground frames, native theme changes, SPA entry/exit, root exclusion, and cleanup. Check real course overviews, chapter lists, articles, and coding exercises when DOM selectors change; preserve course artwork, readable syntax colors, and normal editor controls.
